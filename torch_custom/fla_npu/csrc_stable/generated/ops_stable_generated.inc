// Generated by tools/op_stable_codegen.py -- do not edit by hand.
// Included by csrc_stable/src/stable_ops.cpp (single TU).
#include "thin_stable/at_facade.h"
#include <memory>
#include <optional>
#include <stdexcept>
#include <string>
#include <vector>

namespace {
using torch::stable::Tensor;
using fla_npu_thin::Runtime;
using fla_npu_thin::stable::at_shim::TensorOptions;
using fla_npu_thin::stable::at_shim::empty;
using fla_npu_thin::stable::at_shim::empty_like;
using fla_npu_thin::stable::at_shim::kBFloat16;
using fla_npu_thin::stable::at_shim::kFloat;
using fla_npu_thin::stable::at_shim::kHalf;
using fla_npu_thin::stable::AclIntArrayView;
using fla_npu_thin::stable::AclTensorView;
using fla_npu_thin::stable::LaunchFn;
using fla_npu_thin::stable::TensorMeta;
using fla_npu_thin::stable::aclIntArray;
using fla_npu_thin::stable::aclOpExecutor;
using fla_npu_thin::stable::aclTensor;
using fla_npu_thin::stable::allocate_bytes;
using fla_npu_thin::stable::allocate_like;
using fla_npu_thin::stable::allocate_sizes;
using fla_npu_thin::stable::host_int_values;
using fla_npu_thin::stable::meta_of;
using fla_npu_thin::stable::meta_of_handle;
using fla_npu_thin::stable::size_of;

// Spec alloc expressions are written as at::empty(...)/at::Tensor();
// the facade supplies exactly that surface on top of the stable shims.
namespace shim = fla_npu_thin::stable::at_shim;
}  // namespace

namespace {
inline int64_t chunk_fwd_h_chunk_count(const std::vector<int64_t>& cu_seqlens, const std::vector<int64_t>& chunk_indices, int64_t chunk_size, int64_t total_tokens) {
  if (!chunk_indices.empty()) {
    return static_cast<int64_t>(chunk_indices.size()) / 2;
  }
  if (!cu_seqlens.empty()) {
    int64_t chunks = 0;
    for (size_t i = 0; i + 1 < cu_seqlens.size(); ++i) {
      const int64_t len = cu_seqlens[i + 1] - cu_seqlens[i];
      chunks += (len + chunk_size - 1) / chunk_size;
    }
    return chunks;
  }
  return (total_tokens + chunk_size - 1) / chunk_size;
}
inline int64_t bwd_dhu_chunk_count(const std::vector<int64_t>& chunk_indices, int64_t chunk_size, int64_t total_tokens) {
  if (!chunk_indices.empty()) {
    return static_cast<int64_t>(chunk_indices.size()) / 2;
  }
  return (total_tokens + chunk_size - 1) / chunk_size;
}
inline bool gdn_fwd_head_first(const std::string& layout) {
  return layout != "BSND" && layout != "TND";
}
inline int64_t gdn_fwd_tokens(const shim::Tensor& q, const std::string& layout) {
  return gdn_fwd_head_first(layout) ? q.size(2) : q.size(1);
}
inline int64_t gdn_fwd_kheads(const shim::Tensor& q, const std::string& layout) {
  return gdn_fwd_head_first(layout) ? q.size(1) : q.size(2);
}
inline int64_t gdn_fwd_vheads(const shim::Tensor& v, const std::string& layout) {
  return gdn_fwd_head_first(layout) ? v.size(1) : v.size(2);
}
inline int64_t gdn_fwd_seq_num(const std::vector<int64_t>& cu_seqlens, int64_t batch) {
  return cu_seqlens.empty() ? batch : static_cast<int64_t>(cu_seqlens.size()) - 1;
}
inline int64_t gdn_fwd_chunks(const std::vector<int64_t>& cu_seqlens, const std::vector<int64_t>& chunk_indices, int64_t chunk_size, int64_t tokens) {
  if (!chunk_indices.empty()) {
    return static_cast<int64_t>(chunk_indices.size()) / 2;
  }
  if (!cu_seqlens.empty()) {
    int64_t chunks = 0;
    for (size_t i = 0; i + 1 < cu_seqlens.size(); ++i) {
      const int64_t len = cu_seqlens[i + 1] - cu_seqlens[i];
      chunks += (len + chunk_size - 1) / chunk_size;
    }
    return chunks;
  }
  return (tokens + chunk_size - 1) / chunk_size;
}
inline int64_t gdn_fwd_h_chunk_count(const std::vector<int64_t>& cu_seqlens, const std::vector<int64_t>& chunk_indices, int64_t chunk_size, int64_t total_tokens) {
  if (!chunk_indices.empty()) {
    return static_cast<int64_t>(chunk_indices.size()) / 2;
  }
  if (!cu_seqlens.empty()) {
    int64_t chunks = 0;
    for (size_t i = 0; i + 1 < cu_seqlens.size(); ++i) {
      const int64_t len = cu_seqlens[i + 1] - cu_seqlens[i];
      chunks += (len + chunk_size - 1) / chunk_size;
    }
    return chunks;
  }
  return (total_tokens + chunk_size - 1) / chunk_size;
}
inline bool kda_fwd_rank3(const std::string& layout) {
  return layout == "TND" || layout == "NTD";
}
inline bool kda_fwd_seq_major(const std::string& layout) {
  return layout == "BSND" || layout == "TND";
}
inline int64_t kda_fwd_tokens(const shim::Tensor& q, const std::string& layout) {
  if (kda_fwd_rank3(layout)) {
    return kda_fwd_seq_major(layout) ? q.size(0) : q.size(1);
  }
  return kda_fwd_seq_major(layout) ? q.size(1) : q.size(2);
}
inline int64_t kda_fwd_vheads(const shim::Tensor& v, const std::string& layout) {
  if (kda_fwd_rank3(layout)) {
    return kda_fwd_seq_major(layout) ? v.size(1) : v.size(0);
  }
  return kda_fwd_seq_major(layout) ? v.size(2) : v.size(1);
}
inline int64_t kda_fwd_batch(const shim::Tensor& q, const std::string& layout) {
  return kda_fwd_rank3(layout) ? 1 : q.size(0);
}
inline int64_t kda_fwd_kdim(const shim::Tensor& q, const std::string& layout) {
  return kda_fwd_rank3(layout) ? q.size(2) : q.size(3);
}
inline int64_t kda_fwd_vdim(const shim::Tensor& v, const std::string& layout) {
  return kda_fwd_rank3(layout) ? v.size(2) : v.size(3);
}
inline int64_t kda_fwd_seq_num(const std::vector<int64_t>& cu_seqlens, int64_t batch) {
  return cu_seqlens.empty() ? batch : static_cast<int64_t>(cu_seqlens.size()) - 1;
}
inline int64_t kda_fwd_chunks(const std::vector<int64_t>& cu_seqlens, const std::vector<int64_t>& chunk_indices, int64_t chunk_size, int64_t tokens) {
  if (!chunk_indices.empty()) {
    return static_cast<int64_t>(chunk_indices.size()) / 2;
  }
  if (!cu_seqlens.empty()) {
    int64_t chunks = 0;
    for (size_t i = 0; i + 1 < cu_seqlens.size(); ++i) {
      const int64_t len = cu_seqlens[i + 1] - cu_seqlens[i];
      chunks += (len + chunk_size - 1) / chunk_size;
    }
    return chunks;
  }
  return (tokens + chunk_size - 1) / chunk_size;
}
inline std::vector<int64_t> kda_fwd_attn_sizes(const shim::Tensor& q, const shim::Tensor& v, const std::string& layout) {
  if (kda_fwd_rank3(layout)) {
    return {kda_fwd_tokens(q, layout), kda_fwd_vheads(v, layout), kda_fwd_vdim(v, layout)};
  }
  return {kda_fwd_batch(q, layout), kda_fwd_tokens(q, layout), kda_fwd_vheads(v, layout), kda_fwd_vdim(v, layout)};
}
inline std::vector<int64_t> kda_fwd_khead_sizes(const shim::Tensor& q, const shim::Tensor& v, const std::string& layout) {
  if (kda_fwd_rank3(layout)) {
    return {kda_fwd_vheads(v, layout), kda_fwd_tokens(q, layout), kda_fwd_kdim(q, layout)};
  }
  return {kda_fwd_batch(q, layout), kda_fwd_vheads(v, layout), kda_fwd_tokens(q, layout), kda_fwd_kdim(q, layout)};
}
inline std::vector<int64_t> kda_fwd_vhead_sizes(const shim::Tensor& q, const shim::Tensor& v, const std::string& layout) {
  if (kda_fwd_rank3(layout)) {
    return {kda_fwd_vheads(v, layout), kda_fwd_tokens(q, layout), kda_fwd_vdim(v, layout)};
  }
  return {kda_fwd_batch(q, layout), kda_fwd_vheads(v, layout), kda_fwd_tokens(q, layout), kda_fwd_vdim(v, layout)};
}
inline std::vector<int64_t> kda_fwd_matrix_sizes(const shim::Tensor& q, const shim::Tensor& v, const std::string& layout, int64_t chunk_size) {
  if (kda_fwd_rank3(layout)) {
    return {kda_fwd_vheads(v, layout), kda_fwd_tokens(q, layout), chunk_size};
  }
  return {kda_fwd_batch(q, layout), kda_fwd_vheads(v, layout), kda_fwd_tokens(q, layout), chunk_size};
}
inline std::vector<int64_t> kda_fwd_state_sizes(const shim::Tensor& q, const shim::Tensor& v, const std::string& layout, const std::vector<int64_t>& cu_seqlens, bool state_v_first) {
  return {kda_fwd_seq_num(cu_seqlens, kda_fwd_batch(q, layout)), kda_fwd_vheads(v, layout),
          state_v_first ? kda_fwd_vdim(v, layout) : kda_fwd_kdim(q, layout),
          state_v_first ? kda_fwd_kdim(q, layout) : kda_fwd_vdim(v, layout)};
}
inline std::vector<int64_t> kda_fwd_h_sizes(const shim::Tensor& q, const shim::Tensor& v, const std::string& layout, const std::vector<int64_t>& cu_seqlens, const std::vector<int64_t>& chunk_indices, int64_t chunk_size, bool state_v_first) {
  std::vector<int64_t> sizes;
  if (!kda_fwd_rank3(layout)) {
    sizes.push_back(kda_fwd_batch(q, layout));
  }
  sizes.push_back(kda_fwd_chunks(cu_seqlens, chunk_indices, chunk_size, kda_fwd_tokens(q, layout)));
  sizes.push_back(kda_fwd_vheads(v, layout));
  sizes.push_back(state_v_first ? kda_fwd_vdim(v, layout) : kda_fwd_kdim(q, layout));
  sizes.push_back(state_v_first ? kda_fwd_kdim(q, layout) : kda_fwd_vdim(v, layout));
  return sizes;
}
}  // namespace

// ---- generated: npu_causal_conv1d_bwd ----
inline const char* npu_causal_conv1d_bwd_input_layout_name(int64_t code) {
  switch (code) {
    case 0: return "BSND";
    case 1: return "BNSD";
    case 2: return "NTD";
    case 3: return "TND";
    default:
      throw std::runtime_error(
          "npu_causal_conv1d_bwd: bad input_layout code " + std::to_string(code));
  }
}
using npu_causal_conv1d_bwd_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    int64_t,
    const char*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_causal_conv1d_bwd =
    "npu_causal_conv1d_bwd(Tensor x, Tensor? y, Tensor weight, Tensor dy, Tensor? initial_state, Tensor? dht, Tensor? query_start_loc, int activation, int input_layout, int stream) -> (Tensor, Tensor, Tensor, Tensor)";

void boxed_npu_causal_conv1d_bwd(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor x_t = to<Tensor>(stack[0]);
  const TensorMeta x_meta = meta_of(x_t);
  const shim::Tensor x(x_t);
  const auto y_t = to<std::optional<Tensor>>(stack[1]);
  const TensorMeta y_meta = y_t.has_value() ? meta_of(*y_t) : TensorMeta();
  const std::optional<shim::Tensor> y = y_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*y_t)) : std::nullopt;
  const Tensor weight_t = to<Tensor>(stack[2]);
  const TensorMeta weight_meta = meta_of(weight_t);
  const shim::Tensor weight(weight_t);
  const Tensor dy_t = to<Tensor>(stack[3]);
  const TensorMeta dy_meta = meta_of(dy_t);
  const shim::Tensor dy(dy_t);
  const auto initial_state_t = to<std::optional<Tensor>>(stack[4]);
  const TensorMeta initial_state_meta = initial_state_t.has_value() ? meta_of(*initial_state_t) : TensorMeta();
  const std::optional<shim::Tensor> initial_state = initial_state_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*initial_state_t)) : std::nullopt;
  const auto dht_t = to<std::optional<Tensor>>(stack[5]);
  const TensorMeta dht_meta = dht_t.has_value() ? meta_of(*dht_t) : TensorMeta();
  const std::optional<shim::Tensor> dht = dht_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*dht_t)) : std::nullopt;
  const auto query_start_loc_t = to<std::optional<Tensor>>(stack[6]);
  const std::vector<int64_t> query_start_loc = query_start_loc_t.has_value() ? host_int_values(query_start_loc_t->get()) : std::vector<int64_t>();
  AclIntArrayView query_start_loc_view(query_start_loc);
  const int64_t activation = to<int64_t>(stack[7]);
  const int64_t input_layout_code = to<int64_t>(stack[8]);
  const std::string input_layout = npu_causal_conv1d_bwd_input_layout_name(input_layout_code);
  const int64_t stream = to<int64_t>(stack[9]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_causal_conv1d_bwd_GetWorkspaceFn>(
      rt.symbol("aclnnCausalConv1dBwdGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnCausalConv1dBwd"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<shim::Tensor> outputs;
  outputs.push_back(shim::empty_like(x));
  outputs.push_back(shim::empty({weight.size(0), weight.size(1)}, weight.options()));
  outputs.push_back(shim::empty({weight.size(1)}, weight.options()));
  outputs.push_back(shim::empty({(input_layout == "TND" || input_layout == "NTD") ? (query_start_loc.empty() ? int64_t{0} : static_cast<int64_t>(query_start_loc.size() - 1)) : x.size(0), weight.size(0), weight.size(1)}, x.options()));

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(x_meta));
  views.push_back(std::make_unique<AclTensorView>(y_meta));
  views.push_back(std::make_unique<AclTensorView>(weight_meta));
  views.push_back(std::make_unique<AclTensorView>(dy_meta));
  views.push_back(std::make_unique<AclTensorView>(initial_state_meta));
  views.push_back(std::make_unique<AclTensorView>(dht_meta));
  views.push_back(std::make_unique<AclTensorView>(outputs[0].defined() ? meta_of(outputs[0].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[1].defined() ? meta_of(outputs[1].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[2].defined() ? meta_of(outputs[2].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[3].defined() ? meta_of(outputs[3].tensor()) : TensorMeta()));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      query_start_loc_view.get(),
      activation,
      input_layout.c_str(),
      views[6]->get(),
      views[7]->get(),
      views[8]->get(),
      views[9]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnCausalConv1dBwdGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   x_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnCausalConv1dBwd failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0].tensor());
  stack[1] = from(outputs[1].tensor());
  stack[2] = from(outputs[2].tensor());
  stack[3] = from(outputs[3].tensor());
}

// ---- generated: npu_chunk_bwd_dqkwg ----
using npu_chunk_bwd_dqkwg_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    const aclTensor*,
    const aclTensor*,
    float,
    int64_t,
    bool,
    bool,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_chunk_bwd_dqkwg =
    "npu_chunk_bwd_dqkwg(Tensor q, Tensor k, Tensor v, Tensor g, Tensor h, Tensor dox, Tensor dh, Tensor dv, Tensor? cu_seqlens, Tensor? chunk_indices, Tensor? w, Tensor? g_gamma, float scale, int chunk_size, bool use_exp2, bool transpose_state_layout, int stream) -> (Tensor, Tensor, Tensor, Tensor)";

void boxed_npu_chunk_bwd_dqkwg(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor q_t = to<Tensor>(stack[0]);
  const TensorMeta q_meta = meta_of(q_t);
  const Tensor k_t = to<Tensor>(stack[1]);
  const TensorMeta k_meta = meta_of(k_t);
  const Tensor v_t = to<Tensor>(stack[2]);
  const TensorMeta v_meta = meta_of(v_t);
  const Tensor g_t = to<Tensor>(stack[3]);
  const TensorMeta g_meta = meta_of(g_t);
  const Tensor h_t = to<Tensor>(stack[4]);
  const TensorMeta h_meta = meta_of(h_t);
  const Tensor dox_t = to<Tensor>(stack[5]);
  const TensorMeta dox_meta = meta_of(dox_t);
  const Tensor dh_t = to<Tensor>(stack[6]);
  const TensorMeta dh_meta = meta_of(dh_t);
  const Tensor dv_t = to<Tensor>(stack[7]);
  const TensorMeta dv_meta = meta_of(dv_t);
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[8]);
  const std::vector<int64_t> cu_seqlens_values = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens_values);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[9]);
  const std::vector<int64_t> chunk_indices_values = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices_values);
  const auto w_t = to<std::optional<Tensor>>(stack[10]);
  const TensorMeta w_meta = w_t.has_value() ? meta_of(*w_t) : TensorMeta();
  const auto g_gamma_t = to<std::optional<Tensor>>(stack[11]);
  const TensorMeta g_gamma_meta = g_gamma_t.has_value() ? meta_of(*g_gamma_t) : TensorMeta();
  const double scale = to<double>(stack[12]);
  const int64_t chunk_size = to<int64_t>(stack[13]);
  const bool use_exp2 = to<bool>(stack[14]);
  const bool transpose_state_layout = to<bool>(stack[15]);
  const int64_t stream = to<int64_t>(stack[16]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_chunk_bwd_dqkwg_GetWorkspaceFn>(
      rt.symbol("aclnnChunkBwdDqkwgGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnChunkBwdDqkwg"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<Tensor> outputs;
  std::vector<bool> output_present;
  outputs.push_back(allocate_like(q_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(k_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_sizes({size_of(q_meta, 0), size_of(v_meta, 1), size_of(q_meta, 2), size_of(q_meta, 3)}, q_meta.scalar_type, q_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(g_meta));
  output_present.push_back(true);

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(q_meta));
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(v_meta));
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(h_meta));
  views.push_back(std::make_unique<AclTensorView>(dox_meta));
  views.push_back(std::make_unique<AclTensorView>(dh_meta));
  views.push_back(std::make_unique<AclTensorView>(dv_meta));
  views.push_back(std::make_unique<AclTensorView>(w_meta));
  views.push_back(std::make_unique<AclTensorView>(g_gamma_meta));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[0])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[1])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[2])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[3])));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      views[6]->get(),
      views[7]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      views[8]->get(),
      views[9]->get(),
      scale,
      chunk_size,
      use_exp2,
      transpose_state_layout,
      views[10]->get(),
      views[11]->get(),
      views[12]->get(),
      views[13]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkBwdDqkwgGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   q_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkBwdDqkwg failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0]);
  stack[1] = from(outputs[1]);
  stack[2] = from(outputs[2]);
  stack[3] = from(outputs[3]);
}

// ---- generated: npu_chunk_bwd_dv_local ----
using npu_chunk_bwd_dv_local_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    double,
    int64_t,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_chunk_bwd_dv_local =
    "npu_chunk_bwd_dv_local(Tensor q, Tensor k, Tensor d_o, Tensor g, Tensor? g_gamma, Tensor? A, Tensor? cu_seqlens, Tensor? chunk_indices, float scale, int chunk_size, int stream) -> Tensor";

void boxed_npu_chunk_bwd_dv_local(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor q_t = to<Tensor>(stack[0]);
  const TensorMeta q_meta = meta_of(q_t);
  const Tensor k_t = to<Tensor>(stack[1]);
  const TensorMeta k_meta = meta_of(k_t);
  const Tensor d_o_t = to<Tensor>(stack[2]);
  const TensorMeta d_o_meta = meta_of(d_o_t);
  const Tensor g_t = to<Tensor>(stack[3]);
  const TensorMeta g_meta = meta_of(g_t);
  const auto g_gamma_t = to<std::optional<Tensor>>(stack[4]);
  const TensorMeta g_gamma_meta = g_gamma_t.has_value() ? meta_of(*g_gamma_t) : TensorMeta();
  const auto A_t = to<std::optional<Tensor>>(stack[5]);
  const TensorMeta A_meta = A_t.has_value() ? meta_of(*A_t) : TensorMeta();
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[6]);
  const std::vector<int64_t> cu_seqlens_values = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens_values);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[7]);
  const std::vector<int64_t> chunk_indices_values = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices_values);
  const double scale = to<double>(stack[8]);
  const int64_t chunk_size = to<int64_t>(stack[9]);
  const int64_t stream = to<int64_t>(stack[10]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_chunk_bwd_dv_local_GetWorkspaceFn>(
      rt.symbol("aclnnChunkBwdDvLocalGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnChunkBwdDvLocal"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<Tensor> outputs;
  std::vector<bool> output_present;
  outputs.push_back(allocate_like(d_o_meta));
  output_present.push_back(true);

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(q_meta));
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(d_o_meta));
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(g_gamma_meta));
  views.push_back(std::make_unique<AclTensorView>(A_meta));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[0])));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      scale,
      chunk_size,
      views[6]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkBwdDvLocalGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   q_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkBwdDvLocal failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0]);
}

// ---- generated: npu_chunk_fwd_h ----
using npu_chunk_fwd_h_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    bool,
    int64_t,
    bool,
    const aclIntArray*,
    const aclIntArray*,
    bool,
    bool,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_chunk_fwd_h =
    "npu_chunk_fwd_h(Tensor k, Tensor w, Tensor u, Tensor? g, Tensor? gk, Tensor? initial_state, bool output_final_state, int chunk_size, bool save_new_value, Tensor? cu_seqlens, Tensor? chunk_indices, bool use_exp2, bool state_v_first, int stream) -> (Tensor, Tensor, Tensor?)";

void boxed_npu_chunk_fwd_h(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor k_t = to<Tensor>(stack[0]);
  const TensorMeta k_meta = meta_of(k_t);
  const shim::Tensor k(k_t);
  const Tensor w_t = to<Tensor>(stack[1]);
  const TensorMeta w_meta = meta_of(w_t);
  const shim::Tensor w(w_t);
  const Tensor u_t = to<Tensor>(stack[2]);
  const TensorMeta u_meta = meta_of(u_t);
  const shim::Tensor u(u_t);
  const auto g_t = to<std::optional<Tensor>>(stack[3]);
  const TensorMeta g_meta = g_t.has_value() ? meta_of(*g_t) : TensorMeta();
  const std::optional<shim::Tensor> g = g_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*g_t)) : std::nullopt;
  const auto gk_t = to<std::optional<Tensor>>(stack[4]);
  const TensorMeta gk_meta = gk_t.has_value() ? meta_of(*gk_t) : TensorMeta();
  const std::optional<shim::Tensor> gk = gk_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*gk_t)) : std::nullopt;
  const auto initial_state_t = to<std::optional<Tensor>>(stack[5]);
  const TensorMeta initial_state_meta = initial_state_t.has_value() ? meta_of(*initial_state_t) : TensorMeta();
  const std::optional<shim::Tensor> initial_state = initial_state_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*initial_state_t)) : std::nullopt;
  const bool output_final_state = to<bool>(stack[6]);
  const int64_t chunk_size = to<int64_t>(stack[7]);
  const bool save_new_value = to<bool>(stack[8]);
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[9]);
  const std::vector<int64_t> cu_seqlens = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[10]);
  const std::vector<int64_t> chunk_indices = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices);
  const bool use_exp2 = to<bool>(stack[11]);
  const bool state_v_first = to<bool>(stack[12]);
  const int64_t stream = to<int64_t>(stack[13]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_chunk_fwd_h_GetWorkspaceFn>(
      rt.symbol("aclnnChunkFwdHGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnChunkFwdH"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<shim::Tensor> outputs;
  outputs.push_back(shim::empty({k.size(0), u.size(1), chunk_fwd_h_chunk_count(cu_seqlens, chunk_indices, chunk_size, k.size(2)), (state_v_first ? u.size(3) : k.size(3)), (state_v_first ? k.size(3) : u.size(3))}, k.options()));
  outputs.push_back(shim::empty_like(u));
  if (output_final_state) {
    outputs.push_back((initial_state.has_value() && initial_state->defined()) ? shim::empty({(cu_seqlens.empty() ? k.size(0) : static_cast<int64_t>(cu_seqlens.size() - 1)), u.size(1), (state_v_first ? u.size(3) : k.size(3)), (state_v_first ? k.size(3) : u.size(3))}, initial_state->options()) : shim::empty({(cu_seqlens.empty() ? k.size(0) : static_cast<int64_t>(cu_seqlens.size() - 1)), u.size(1), (state_v_first ? u.size(3) : k.size(3)), (state_v_first ? k.size(3) : u.size(3))}, k.options().dtype(shim::kFloat)));
  } else {
    outputs.push_back(shim::Tensor());
  }

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(w_meta));
  views.push_back(std::make_unique<AclTensorView>(u_meta));
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(gk_meta));
  views.push_back(std::make_unique<AclTensorView>(initial_state_meta));
  views.push_back(std::make_unique<AclTensorView>(outputs[0].defined() ? meta_of(outputs[0].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[1].defined() ? meta_of(outputs[1].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[2].defined() ? meta_of(outputs[2].tensor()) : TensorMeta()));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      output_final_state,
      chunk_size,
      save_new_value,
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      use_exp2,
      state_v_first,
      views[6]->get(),
      views[7]->get(),
      views[8]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkFwdHGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   k_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkFwdH failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0].tensor());
  stack[1] = from(outputs[1].tensor());
  stack[2] = outputs[2].defined() ? from(std::optional<Tensor>(outputs[2].tensor())) : from(std::nullopt);
}

// ---- generated: npu_chunk_fwd_o ----
inline const char* npu_chunk_fwd_o_output_layout_name(int64_t code) {
  switch (code) {
    case 0: return "BNSD";
    case 1: return "BSND";
    case 2: return "TND";
    case 3: return "NTD";
    default:
      throw std::runtime_error(
          "npu_chunk_fwd_o: bad output_layout code " + std::to_string(code));
  }
}
using npu_chunk_fwd_o_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    double,
    int64_t,
    bool,
    bool,
    const char*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_chunk_fwd_o =
    "npu_chunk_fwd_o(Tensor q, Tensor k, Tensor v, Tensor h, Tensor? g, Tensor? cu_seqlens, Tensor? chunk_indices, float scale, int chunk_size, bool use_exp2, bool transpose_state_layout, int output_layout, int stream) -> Tensor";

void boxed_npu_chunk_fwd_o(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor q_t = to<Tensor>(stack[0]);
  const TensorMeta q_meta = meta_of(q_t);
  const shim::Tensor q(q_t);
  const Tensor k_t = to<Tensor>(stack[1]);
  const TensorMeta k_meta = meta_of(k_t);
  const shim::Tensor k(k_t);
  const Tensor v_t = to<Tensor>(stack[2]);
  const TensorMeta v_meta = meta_of(v_t);
  const shim::Tensor v(v_t);
  const Tensor h_t = to<Tensor>(stack[3]);
  const TensorMeta h_meta = meta_of(h_t);
  const shim::Tensor h(h_t);
  const auto g_t = to<std::optional<Tensor>>(stack[4]);
  const TensorMeta g_meta = g_t.has_value() ? meta_of(*g_t) : TensorMeta();
  const std::optional<shim::Tensor> g = g_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*g_t)) : std::nullopt;
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[5]);
  const std::vector<int64_t> cu_seqlens = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[6]);
  const std::vector<int64_t> chunk_indices = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices);
  const double scale = to<double>(stack[7]);
  const int64_t chunk_size = to<int64_t>(stack[8]);
  const bool use_exp2 = to<bool>(stack[9]);
  const bool transpose_state_layout = to<bool>(stack[10]);
  const int64_t output_layout_code = to<int64_t>(stack[11]);
  const std::string output_layout = npu_chunk_fwd_o_output_layout_name(output_layout_code);
  const int64_t stream = to<int64_t>(stack[12]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_chunk_fwd_o_GetWorkspaceFn>(
      rt.symbol("aclnnChunkFwdOGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnChunkFwdO"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<shim::Tensor> outputs;
  outputs.push_back((output_layout == "BNSD") ? shim::empty({v.size(0), v.size(1), v.size(2), v.size(3)}, v.options()) : (output_layout == "BSND") ? shim::empty({v.size(0), v.size(2), v.size(1), v.size(3)}, v.options()) : (output_layout == "TND") ? shim::empty({v.size(2), v.size(1), v.size(3)}, v.options()) : shim::empty({v.size(1), v.size(2), v.size(3)}, v.options()));

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(q_meta));
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(v_meta));
  views.push_back(std::make_unique<AclTensorView>(h_meta));
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(outputs[0].defined() ? meta_of(outputs[0].tensor()) : TensorMeta()));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      scale,
      chunk_size,
      use_exp2,
      transpose_state_layout,
      output_layout.c_str(),
      views[5]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkFwdOGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   q_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkFwdO failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0].tensor());
}

// ---- generated: npu_chunk_gated_delta_rule_bwd_dhu ----
using npu_chunk_gated_delta_rule_bwd_dhu_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    double,
    int64_t,
    bool,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_chunk_gated_delta_rule_bwd_dhu =
    "npu_chunk_gated_delta_rule_bwd_dhu(Tensor q, Tensor k, Tensor w, Tensor d_o, Tensor dv, Tensor? g, Tensor? gK, Tensor? h0, Tensor? dht, Tensor? cu_seqlens, Tensor? chunk_indices, float scale, int chunk_size, bool use_exp2, int stream) -> (Tensor, Tensor?, Tensor)";

void boxed_npu_chunk_gated_delta_rule_bwd_dhu(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor q_t = to<Tensor>(stack[0]);
  const TensorMeta q_meta = meta_of(q_t);
  const shim::Tensor q(q_t);
  const Tensor k_t = to<Tensor>(stack[1]);
  const TensorMeta k_meta = meta_of(k_t);
  const shim::Tensor k(k_t);
  const Tensor w_t = to<Tensor>(stack[2]);
  const TensorMeta w_meta = meta_of(w_t);
  const shim::Tensor w(w_t);
  const Tensor d_o_t = to<Tensor>(stack[3]);
  const TensorMeta d_o_meta = meta_of(d_o_t);
  const shim::Tensor d_o(d_o_t);
  const Tensor dv_t = to<Tensor>(stack[4]);
  const TensorMeta dv_meta = meta_of(dv_t);
  const shim::Tensor dv(dv_t);
  const auto g_t = to<std::optional<Tensor>>(stack[5]);
  const TensorMeta g_meta = g_t.has_value() ? meta_of(*g_t) : TensorMeta();
  const std::optional<shim::Tensor> g = g_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*g_t)) : std::nullopt;
  const auto gK_t = to<std::optional<Tensor>>(stack[6]);
  const TensorMeta gK_meta = gK_t.has_value() ? meta_of(*gK_t) : TensorMeta();
  const std::optional<shim::Tensor> gK = gK_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*gK_t)) : std::nullopt;
  const auto h0_t = to<std::optional<Tensor>>(stack[7]);
  const TensorMeta h0_meta = h0_t.has_value() ? meta_of(*h0_t) : TensorMeta();
  const std::optional<shim::Tensor> h0 = h0_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*h0_t)) : std::nullopt;
  const auto dht_t = to<std::optional<Tensor>>(stack[8]);
  const TensorMeta dht_meta = dht_t.has_value() ? meta_of(*dht_t) : TensorMeta();
  const std::optional<shim::Tensor> dht = dht_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*dht_t)) : std::nullopt;
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[9]);
  const std::vector<int64_t> cu_seqlens = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[10]);
  const std::vector<int64_t> chunk_indices = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices);
  const double scale = to<double>(stack[11]);
  const int64_t chunk_size = to<int64_t>(stack[12]);
  const bool use_exp2 = to<bool>(stack[13]);
  const int64_t stream = to<int64_t>(stack[14]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_chunk_gated_delta_rule_bwd_dhu_GetWorkspaceFn>(
      rt.symbol("aclnnChunkGatedDeltaRuleBwdDhuGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnChunkGatedDeltaRuleBwdDhu"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<shim::Tensor> outputs;
  outputs.push_back(shim::empty({q.size(0), dv.size(1), bwd_dhu_chunk_count(chunk_indices, chunk_size, q.size(2)), q.size(3), dv.size(3)}, q.options()));
  if (h0.has_value() && h0->defined()) {
    outputs.push_back(shim::empty({q.size(0), dv.size(1), bwd_dhu_chunk_count(chunk_indices, chunk_size, q.size(2)), q.size(3), dv.size(3)}, q.options()));
  } else {
    outputs.push_back(shim::Tensor());
  }
  outputs.push_back(shim::empty_like(dv));

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(q_meta));
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(w_meta));
  views.push_back(std::make_unique<AclTensorView>(d_o_meta));
  views.push_back(std::make_unique<AclTensorView>(dv_meta));
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(gK_meta));
  views.push_back(std::make_unique<AclTensorView>(h0_meta));
  views.push_back(std::make_unique<AclTensorView>(dht_meta));
  views.push_back(std::make_unique<AclTensorView>(outputs[0].defined() ? meta_of(outputs[0].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[1].defined() ? meta_of(outputs[1].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[2].defined() ? meta_of(outputs[2].tensor()) : TensorMeta()));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      views[6]->get(),
      views[7]->get(),
      views[8]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      scale,
      chunk_size,
      use_exp2,
      views[9]->get(),
      views[10]->get(),
      views[11]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkGatedDeltaRuleBwdDhuGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   q_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkGatedDeltaRuleBwdDhu failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0].tensor());
  stack[1] = outputs[1].defined() ? from(std::optional<Tensor>(outputs[1].tensor())) : from(std::nullopt);
  stack[2] = from(outputs[2].tensor());
}

// ---- generated: npu_chunk_gated_delta_rule_bwd_finalize ----
using npu_chunk_gated_delta_rule_bwd_finalize_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    double,
    int64_t,
    bool,
    bool,
    bool,
    bool,
    bool,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_chunk_gated_delta_rule_bwd_finalize =
    "npu_chunk_gated_delta_rule_bwd_finalize(Tensor q, Tensor k, Tensor v, Tensor v_new, Tensor do, Tensor du, Tensor g, Tensor beta, Tensor h, Tensor dh, Tensor a, Tensor? q_rstd, Tensor? k_rstd, Tensor? beta_raw, Tensor? cu_seqlens, Tensor? chunk_indices, float scale, int chunk_size, bool use_qk_l2_norm_in_kernel, bool use_beta_sigmoid_in_kernel, bool use_gate_in_kernel, bool state_v_first, bool use_exp2, int stream) -> (Tensor, Tensor, Tensor, Tensor, Tensor)";

void boxed_npu_chunk_gated_delta_rule_bwd_finalize(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor q_t = to<Tensor>(stack[0]);
  const TensorMeta q_meta = meta_of(q_t);
  const Tensor k_t = to<Tensor>(stack[1]);
  const TensorMeta k_meta = meta_of(k_t);
  const Tensor v_t = to<Tensor>(stack[2]);
  const TensorMeta v_meta = meta_of(v_t);
  const Tensor v_new_t = to<Tensor>(stack[3]);
  const TensorMeta v_new_meta = meta_of(v_new_t);
  const Tensor d_o_t = to<Tensor>(stack[4]);
  const TensorMeta d_o_meta = meta_of(d_o_t);
  const Tensor du_t = to<Tensor>(stack[5]);
  const TensorMeta du_meta = meta_of(du_t);
  const Tensor g_t = to<Tensor>(stack[6]);
  const TensorMeta g_meta = meta_of(g_t);
  const Tensor beta_t = to<Tensor>(stack[7]);
  const TensorMeta beta_meta = meta_of(beta_t);
  const Tensor h_t = to<Tensor>(stack[8]);
  const TensorMeta h_meta = meta_of(h_t);
  const Tensor dh_t = to<Tensor>(stack[9]);
  const TensorMeta dh_meta = meta_of(dh_t);
  const Tensor a_t = to<Tensor>(stack[10]);
  const TensorMeta a_meta = meta_of(a_t);
  const auto q_rstd_t = to<std::optional<Tensor>>(stack[11]);
  const TensorMeta q_rstd_meta = q_rstd_t.has_value() ? meta_of(*q_rstd_t) : TensorMeta();
  const auto k_rstd_t = to<std::optional<Tensor>>(stack[12]);
  const TensorMeta k_rstd_meta = k_rstd_t.has_value() ? meta_of(*k_rstd_t) : TensorMeta();
  const auto beta_raw_t = to<std::optional<Tensor>>(stack[13]);
  const TensorMeta beta_raw_meta = beta_raw_t.has_value() ? meta_of(*beta_raw_t) : TensorMeta();
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[14]);
  const std::vector<int64_t> cu_seqlens_values = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens_values);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[15]);
  const std::vector<int64_t> chunk_indices_values = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices_values);
  const double scale = to<double>(stack[16]);
  const int64_t chunk_size = to<int64_t>(stack[17]);
  const bool use_qk_l2_norm_in_kernel = to<bool>(stack[18]);
  const bool use_beta_sigmoid_in_kernel = to<bool>(stack[19]);
  const bool use_gate_in_kernel = to<bool>(stack[20]);
  const bool state_v_first = to<bool>(stack[21]);
  const bool use_exp2 = to<bool>(stack[22]);
  const int64_t stream = to<int64_t>(stack[23]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_chunk_gated_delta_rule_bwd_finalize_GetWorkspaceFn>(
      rt.symbol("aclnnChunkGatedDeltaRuleBwdFinalizeGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnChunkGatedDeltaRuleBwdFinalize"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<Tensor> outputs;
  std::vector<bool> output_present;
  outputs.push_back(allocate_like(q_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(k_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(v_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(beta_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(g_meta));
  output_present.push_back(true);

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(q_meta));
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(v_meta));
  views.push_back(std::make_unique<AclTensorView>(v_new_meta));
  views.push_back(std::make_unique<AclTensorView>(d_o_meta));
  views.push_back(std::make_unique<AclTensorView>(du_meta));
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(beta_meta));
  views.push_back(std::make_unique<AclTensorView>(h_meta));
  views.push_back(std::make_unique<AclTensorView>(dh_meta));
  views.push_back(std::make_unique<AclTensorView>(a_meta));
  views.push_back(std::make_unique<AclTensorView>(q_rstd_meta));
  views.push_back(std::make_unique<AclTensorView>(k_rstd_meta));
  views.push_back(std::make_unique<AclTensorView>(beta_raw_meta));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[0])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[1])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[2])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[3])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[4])));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      views[6]->get(),
      views[7]->get(),
      views[8]->get(),
      views[9]->get(),
      views[10]->get(),
      views[11]->get(),
      views[12]->get(),
      views[13]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      scale,
      chunk_size,
      use_qk_l2_norm_in_kernel,
      use_beta_sigmoid_in_kernel,
      use_gate_in_kernel,
      state_v_first,
      use_exp2,
      views[14]->get(),
      views[15]->get(),
      views[16]->get(),
      views[17]->get(),
      views[18]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkGatedDeltaRuleBwdFinalizeGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   q_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkGatedDeltaRuleBwdFinalize failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0]);
  stack[1] = from(outputs[1]);
  stack[2] = from(outputs[2]);
  stack[3] = from(outputs[3]);
  stack[4] = from(outputs[4]);
}

// ---- generated: npu_chunk_gated_delta_rule_fwd ----
inline const char* npu_chunk_gated_delta_rule_fwd_layout_name(int64_t code) {
  switch (code) {
    case 0: return "BSND";
    case 1: return "BNSD";
    case 2: return "NTD";
    case 3: return "TND";
    default:
      throw std::runtime_error(
          "npu_chunk_gated_delta_rule_fwd: bad layout code " + std::to_string(code));
  }
}
using npu_chunk_gated_delta_rule_fwd_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    const char*,
    double,
    int64_t,
    bool,
    bool,
    bool,
    bool,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_chunk_gated_delta_rule_fwd =
    "npu_chunk_gated_delta_rule_fwd(Tensor q, Tensor k, Tensor v, Tensor g, Tensor beta, Tensor? a_log, Tensor? dt_bias, Tensor? initial_state, Tensor? cu_seqlens, Tensor? chunk_indices, int layout, float scale, int chunk_size, bool use_exp2, bool use_qk_l2norm_in_kernel, bool allow_neg_eigval, bool state_v_first, bool output_final_state, bool disable_recompute, bool return_intermediate_states, bool use_gate_in_kernel, bool use_beta_sigmoid_in_kernel, int stream) -> (Tensor, Tensor?, Tensor?, Tensor?, Tensor?, Tensor?, Tensor?, Tensor?, Tensor?, Tensor?)";

void boxed_npu_chunk_gated_delta_rule_fwd(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor q_t = to<Tensor>(stack[0]);
  const TensorMeta q_meta = meta_of(q_t);
  const shim::Tensor q(q_t);
  const Tensor k_t = to<Tensor>(stack[1]);
  const TensorMeta k_meta = meta_of(k_t);
  const shim::Tensor k(k_t);
  const Tensor v_t = to<Tensor>(stack[2]);
  const TensorMeta v_meta = meta_of(v_t);
  const shim::Tensor v(v_t);
  const Tensor g_t = to<Tensor>(stack[3]);
  const TensorMeta g_meta = meta_of(g_t);
  const shim::Tensor g(g_t);
  const Tensor beta_t = to<Tensor>(stack[4]);
  const TensorMeta beta_meta = meta_of(beta_t);
  const shim::Tensor beta(beta_t);
  const auto a_log_t = to<std::optional<Tensor>>(stack[5]);
  const TensorMeta a_log_meta = a_log_t.has_value() ? meta_of(*a_log_t) : TensorMeta();
  const std::optional<shim::Tensor> a_log = a_log_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*a_log_t)) : std::nullopt;
  const auto dt_bias_t = to<std::optional<Tensor>>(stack[6]);
  const TensorMeta dt_bias_meta = dt_bias_t.has_value() ? meta_of(*dt_bias_t) : TensorMeta();
  const std::optional<shim::Tensor> dt_bias = dt_bias_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*dt_bias_t)) : std::nullopt;
  const auto initial_state_t = to<std::optional<Tensor>>(stack[7]);
  const TensorMeta initial_state_meta = initial_state_t.has_value() ? meta_of(*initial_state_t) : TensorMeta();
  const std::optional<shim::Tensor> initial_state = initial_state_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*initial_state_t)) : std::nullopt;
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[8]);
  const std::vector<int64_t> cu_seqlens = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[9]);
  const std::vector<int64_t> chunk_indices = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices);
  const int64_t layout_code = to<int64_t>(stack[10]);
  const std::string layout = npu_chunk_gated_delta_rule_fwd_layout_name(layout_code);
  const double scale = to<double>(stack[11]);
  const int64_t chunk_size = to<int64_t>(stack[12]);
  const bool use_exp2 = to<bool>(stack[13]);
  const bool use_qk_l2norm_in_kernel = to<bool>(stack[14]);
  const bool allow_neg_eigval = to<bool>(stack[15]);
  const bool state_v_first = to<bool>(stack[16]);
  const bool output_final_state = to<bool>(stack[17]);
  const bool disable_recompute = to<bool>(stack[18]);
  const bool return_intermediate_states = to<bool>(stack[19]);
  const bool use_gate_in_kernel = to<bool>(stack[20]);
  const bool use_beta_sigmoid_in_kernel = to<bool>(stack[21]);
  const int64_t stream = to<int64_t>(stack[22]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_chunk_gated_delta_rule_fwd_GetWorkspaceFn>(
      rt.symbol("aclnnChunkGatedDeltaRuleFwdGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnChunkGatedDeltaRuleFwd"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<shim::Tensor> outputs;
  outputs.push_back(shim::empty({q.size(0), gdn_fwd_tokens(q, layout), gdn_fwd_vheads(v, layout), v.size(3)}, v.options()));
  if (output_final_state) {
    outputs.push_back(shim::empty({gdn_fwd_seq_num(cu_seqlens, q.size(0)), gdn_fwd_vheads(v, layout), (state_v_first ? v.size(3) : q.size(3)), (state_v_first ? q.size(3) : v.size(3))}, initial_state.has_value() && initial_state->defined() ? initial_state->options() : q.options().dtype(shim::kFloat)));
  } else {
    outputs.push_back(shim::Tensor());
  }
  outputs.push_back(shim::Tensor());
  outputs.push_back(shim::Tensor());
  outputs.push_back(shim::Tensor());
  outputs.push_back(shim::Tensor());
  outputs.push_back(shim::Tensor());
  if (!disable_recompute) {
    outputs.push_back(shim::empty({q.size(0), gdn_fwd_tokens(q, layout), gdn_fwd_vheads(v, layout)}, g.options().dtype(shim::kFloat)));
  } else {
    outputs.push_back(shim::Tensor());
  }
  if (!disable_recompute) {
    outputs.push_back(shim::empty({q.size(0), gdn_fwd_vheads(v, layout), gdn_fwd_tokens(q, layout), chunk_size}, q.options()));
  } else {
    outputs.push_back(shim::Tensor());
  }
  if (return_intermediate_states) {
    outputs.push_back(shim::empty({q.size(0), gdn_fwd_vheads(v, layout), gdn_fwd_chunks(cu_seqlens, chunk_indices, chunk_size, gdn_fwd_tokens(q, layout)), (state_v_first ? v.size(3) : q.size(3)), (state_v_first ? q.size(3) : v.size(3))}, q.options()));
  } else {
    outputs.push_back(shim::Tensor());
  }

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(q_meta));
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(v_meta));
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(beta_meta));
  views.push_back(std::make_unique<AclTensorView>(a_log_meta));
  views.push_back(std::make_unique<AclTensorView>(dt_bias_meta));
  views.push_back(std::make_unique<AclTensorView>(initial_state_meta));
  views.push_back(std::make_unique<AclTensorView>(outputs[0].defined() ? meta_of(outputs[0].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[1].defined() ? meta_of(outputs[1].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[2].defined() ? meta_of(outputs[2].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[3].defined() ? meta_of(outputs[3].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[4].defined() ? meta_of(outputs[4].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[5].defined() ? meta_of(outputs[5].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[6].defined() ? meta_of(outputs[6].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[7].defined() ? meta_of(outputs[7].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[8].defined() ? meta_of(outputs[8].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[9].defined() ? meta_of(outputs[9].tensor()) : TensorMeta()));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      views[6]->get(),
      views[7]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      layout.c_str(),
      scale,
      chunk_size,
      use_exp2,
      use_qk_l2norm_in_kernel,
      allow_neg_eigval,
      state_v_first,
      views[8]->get(),
      views[9]->get(),
      views[10]->get(),
      views[11]->get(),
      views[12]->get(),
      views[13]->get(),
      views[14]->get(),
      views[15]->get(),
      views[16]->get(),
      views[17]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkGatedDeltaRuleFwdGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   q_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkGatedDeltaRuleFwd failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0].tensor());
  stack[1] = outputs[1].defined() ? from(std::optional<Tensor>(outputs[1].tensor())) : from(std::nullopt);
  stack[2] = outputs[2].defined() ? from(std::optional<Tensor>(outputs[2].tensor())) : from(std::nullopt);
  stack[3] = outputs[3].defined() ? from(std::optional<Tensor>(outputs[3].tensor())) : from(std::nullopt);
  stack[4] = outputs[4].defined() ? from(std::optional<Tensor>(outputs[4].tensor())) : from(std::nullopt);
  stack[5] = outputs[5].defined() ? from(std::optional<Tensor>(outputs[5].tensor())) : from(std::nullopt);
  stack[6] = outputs[6].defined() ? from(std::optional<Tensor>(outputs[6].tensor())) : from(std::nullopt);
  stack[7] = outputs[7].defined() ? from(std::optional<Tensor>(outputs[7].tensor())) : from(std::nullopt);
  stack[8] = outputs[8].defined() ? from(std::optional<Tensor>(outputs[8].tensor())) : from(std::nullopt);
  stack[9] = outputs[9].defined() ? from(std::optional<Tensor>(outputs[9].tensor())) : from(std::nullopt);
}

// ---- generated: npu_chunk_gated_delta_rule_fwd_h ----
using npu_chunk_gated_delta_rule_fwd_h_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    bool,
    int64_t,
    const aclIntArray*,
    const aclIntArray*,
    bool,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_chunk_gated_delta_rule_fwd_h =
    "npu_chunk_gated_delta_rule_fwd_h(Tensor k, Tensor w, Tensor u, Tensor? g, Tensor? gk, Tensor? initial_state, bool output_final_state, int chunk_size, Tensor? cu_seqlens, Tensor? chunk_indices, bool state_v_first, int stream) -> (Tensor, Tensor, Tensor?)";

void boxed_npu_chunk_gated_delta_rule_fwd_h(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor k_t = to<Tensor>(stack[0]);
  const TensorMeta k_meta = meta_of(k_t);
  const shim::Tensor k(k_t);
  const Tensor w_t = to<Tensor>(stack[1]);
  const TensorMeta w_meta = meta_of(w_t);
  const shim::Tensor w(w_t);
  const Tensor u_t = to<Tensor>(stack[2]);
  const TensorMeta u_meta = meta_of(u_t);
  const shim::Tensor u(u_t);
  const auto g_t = to<std::optional<Tensor>>(stack[3]);
  const TensorMeta g_meta = g_t.has_value() ? meta_of(*g_t) : TensorMeta();
  const std::optional<shim::Tensor> g = g_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*g_t)) : std::nullopt;
  const auto gk_t = to<std::optional<Tensor>>(stack[4]);
  const TensorMeta gk_meta = gk_t.has_value() ? meta_of(*gk_t) : TensorMeta();
  const std::optional<shim::Tensor> gk = gk_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*gk_t)) : std::nullopt;
  const auto initial_state_t = to<std::optional<Tensor>>(stack[5]);
  const TensorMeta initial_state_meta = initial_state_t.has_value() ? meta_of(*initial_state_t) : TensorMeta();
  const std::optional<shim::Tensor> initial_state = initial_state_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*initial_state_t)) : std::nullopt;
  const bool output_final_state = to<bool>(stack[6]);
  const int64_t chunk_size = to<int64_t>(stack[7]);
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[8]);
  const std::vector<int64_t> cu_seqlens = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[9]);
  const std::vector<int64_t> chunk_indices = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices);
  const bool state_v_first = to<bool>(stack[10]);
  const int64_t stream = to<int64_t>(stack[11]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_chunk_gated_delta_rule_fwd_h_GetWorkspaceFn>(
      rt.symbol("aclnnChunkGatedDeltaRuleFwdHGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnChunkGatedDeltaRuleFwdH"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<shim::Tensor> outputs;
  outputs.push_back(shim::empty({k.size(0), u.size(1), gdn_fwd_h_chunk_count(cu_seqlens, chunk_indices, chunk_size, k.size(2)), (state_v_first ? u.size(3) : k.size(3)), (state_v_first ? k.size(3) : u.size(3))}, k.options()));
  outputs.push_back(shim::empty_like(u));
  if (output_final_state) {
    outputs.push_back((initial_state.has_value() && initial_state->defined()) ? shim::empty({(cu_seqlens.empty() ? k.size(0) : static_cast<int64_t>(cu_seqlens.size() - 1)), u.size(1), (state_v_first ? u.size(3) : k.size(3)), (state_v_first ? k.size(3) : u.size(3))}, initial_state->options()) : shim::empty({(cu_seqlens.empty() ? k.size(0) : static_cast<int64_t>(cu_seqlens.size() - 1)), u.size(1), (state_v_first ? u.size(3) : k.size(3)), (state_v_first ? k.size(3) : u.size(3))}, k.options().dtype(shim::kFloat)));
  } else {
    outputs.push_back(shim::Tensor());
  }

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(w_meta));
  views.push_back(std::make_unique<AclTensorView>(u_meta));
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(gk_meta));
  views.push_back(std::make_unique<AclTensorView>(initial_state_meta));
  views.push_back(std::make_unique<AclTensorView>(outputs[0].defined() ? meta_of(outputs[0].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[1].defined() ? meta_of(outputs[1].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[2].defined() ? meta_of(outputs[2].tensor()) : TensorMeta()));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      output_final_state,
      chunk_size,
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      state_v_first,
      views[6]->get(),
      views[7]->get(),
      views[8]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkGatedDeltaRuleFwdHGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   k_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkGatedDeltaRuleFwdH failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0].tensor());
  stack[1] = from(outputs[1].tensor());
  stack[2] = outputs[2].defined() ? from(std::optional<Tensor>(outputs[2].tensor())) : from(std::nullopt);
}

// ---- generated: npu_chunk_gated_delta_rule_fwd_prepare ----
using npu_chunk_gated_delta_rule_fwd_prepare_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    int64_t,
    bool,
    bool,
    bool,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_chunk_gated_delta_rule_fwd_prepare =
    "npu_chunk_gated_delta_rule_fwd_prepare(Tensor q, Tensor k, Tensor v, Tensor g, Tensor beta, Tensor? a_log, Tensor? dt_bias, Tensor? cu_seqlens, Tensor? chunk_indices, int chunk_size, bool allow_neg_eigval, bool use_exp2, bool output_a, bool use_beta_sigmoid_in_kernel, bool use_gate_in_kernel, int stream) -> (Tensor, Tensor, Tensor, Tensor, Tensor, Tensor, Tensor, Tensor, Tensor?)";

void boxed_npu_chunk_gated_delta_rule_fwd_prepare(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor q_t = to<Tensor>(stack[0]);
  const TensorMeta q_meta = meta_of(q_t);
  const shim::Tensor q(q_t);
  const Tensor k_t = to<Tensor>(stack[1]);
  const TensorMeta k_meta = meta_of(k_t);
  const shim::Tensor k(k_t);
  const Tensor v_t = to<Tensor>(stack[2]);
  const TensorMeta v_meta = meta_of(v_t);
  const shim::Tensor v(v_t);
  const Tensor g_t = to<Tensor>(stack[3]);
  const TensorMeta g_meta = meta_of(g_t);
  const shim::Tensor g(g_t);
  const Tensor beta_t = to<Tensor>(stack[4]);
  const TensorMeta beta_meta = meta_of(beta_t);
  const shim::Tensor beta(beta_t);
  const auto a_log_t = to<std::optional<Tensor>>(stack[5]);
  const TensorMeta a_log_meta = a_log_t.has_value() ? meta_of(*a_log_t) : TensorMeta();
  const std::optional<shim::Tensor> a_log = a_log_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*a_log_t)) : std::nullopt;
  const auto dt_bias_t = to<std::optional<Tensor>>(stack[6]);
  const TensorMeta dt_bias_meta = dt_bias_t.has_value() ? meta_of(*dt_bias_t) : TensorMeta();
  const std::optional<shim::Tensor> dt_bias = dt_bias_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*dt_bias_t)) : std::nullopt;
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[7]);
  const std::vector<int64_t> cu_seqlens = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[8]);
  const std::vector<int64_t> chunk_indices = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices);
  const int64_t chunk_size = to<int64_t>(stack[9]);
  const bool allow_neg_eigval = to<bool>(stack[10]);
  const bool use_exp2 = to<bool>(stack[11]);
  const bool output_a = to<bool>(stack[12]);
  const bool use_beta_sigmoid_in_kernel = to<bool>(stack[13]);
  const bool use_gate_in_kernel = to<bool>(stack[14]);
  const int64_t stream = to<int64_t>(stack[15]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_chunk_gated_delta_rule_fwd_prepare_GetWorkspaceFn>(
      rt.symbol("aclnnChunkGatedDeltaRuleFwdPrepareGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnChunkGatedDeltaRuleFwdPrepare"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<shim::Tensor> outputs;
  outputs.push_back(shim::empty({q.size(0), v.size(1), q.size(2)}, q.options().dtype(shim::kFloat)));
  outputs.push_back(shim::empty({q.size(0), v.size(1), q.size(2), k.size(3)}, k.options()));
  outputs.push_back(shim::empty_like(v));
  outputs.push_back(shim::empty({q.size(0), v.size(1), q.size(2), chunk_size}, k.options()));
  outputs.push_back(shim::empty_like(q));
  outputs.push_back(shim::empty_like(k));
  outputs.push_back(shim::empty({q.size(0), q.size(1), q.size(2)}, q.options().dtype(shim::kFloat)));
  outputs.push_back(shim::empty({k.size(0), k.size(1), k.size(2)}, k.options().dtype(shim::kFloat)));
  if (use_beta_sigmoid_in_kernel) {
    outputs.push_back(shim::empty({q.size(0), v.size(1), q.size(2)}, beta.options().dtype(shim::kFloat)));
  } else {
    outputs.push_back(shim::Tensor());
  }

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(q_meta));
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(v_meta));
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(beta_meta));
  views.push_back(std::make_unique<AclTensorView>(a_log_meta));
  views.push_back(std::make_unique<AclTensorView>(dt_bias_meta));
  views.push_back(std::make_unique<AclTensorView>(outputs[0].defined() ? meta_of(outputs[0].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[1].defined() ? meta_of(outputs[1].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[2].defined() ? meta_of(outputs[2].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[3].defined() ? meta_of(outputs[3].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[4].defined() ? meta_of(outputs[4].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[5].defined() ? meta_of(outputs[5].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[6].defined() ? meta_of(outputs[6].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[7].defined() ? meta_of(outputs[7].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[8].defined() ? meta_of(outputs[8].tensor()) : TensorMeta()));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      views[6]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      chunk_size,
      allow_neg_eigval,
      use_exp2,
      output_a,
      views[7]->get(),
      views[8]->get(),
      views[9]->get(),
      views[10]->get(),
      views[11]->get(),
      views[12]->get(),
      views[13]->get(),
      views[14]->get(),
      views[15]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkGatedDeltaRuleFwdPrepareGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   q_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkGatedDeltaRuleFwdPrepare failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0].tensor());
  stack[1] = from(outputs[1].tensor());
  stack[2] = from(outputs[2].tensor());
  stack[3] = from(outputs[3].tensor());
  stack[4] = from(outputs[4].tensor());
  stack[5] = from(outputs[5].tensor());
  stack[6] = from(outputs[6].tensor());
  stack[7] = from(outputs[7].tensor());
  stack[8] = outputs[8].defined() ? from(std::optional<Tensor>(outputs[8].tensor())) : from(std::nullopt);
}

// ---- generated: npu_chunk_kda_bwd ----
using npu_chunk_kda_bwd_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    double,
    int64_t,
    bool,
    bool,
    double,
    bool,
    bool,
    bool,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_chunk_kda_bwd =
    "npu_chunk_kda_bwd(Tensor q, Tensor k, Tensor v, Tensor beta, Tensor gk, Tensor Aqk, Tensor Akk, Tensor? w, Tensor? qg, Tensor? kg, Tensor? v_new, Tensor? h, Tensor d_o, Tensor? raw_g, Tensor? A_log, Tensor? dt_bias, Tensor? initial_state, Tensor? dht, Tensor? cu_seqlens, Tensor? chunk_indices, float scale, int chunk_size, bool safe_gate, bool use_gate_in_kernel, float lower_bound, bool disable_recompute, bool use_exp2, bool state_v_first, int stream) -> (Tensor, Tensor, Tensor, Tensor, Tensor, Tensor?, Tensor?, Tensor?)";

void boxed_npu_chunk_kda_bwd(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor q_t = to<Tensor>(stack[0]);
  const TensorMeta q_meta = meta_of(q_t);
  const shim::Tensor q(q_t);
  const Tensor k_t = to<Tensor>(stack[1]);
  const TensorMeta k_meta = meta_of(k_t);
  const shim::Tensor k(k_t);
  const Tensor v_t = to<Tensor>(stack[2]);
  const TensorMeta v_meta = meta_of(v_t);
  const shim::Tensor v(v_t);
  const Tensor beta_t = to<Tensor>(stack[3]);
  const TensorMeta beta_meta = meta_of(beta_t);
  const shim::Tensor beta(beta_t);
  const Tensor gk_t = to<Tensor>(stack[4]);
  const TensorMeta gk_meta = meta_of(gk_t);
  const shim::Tensor gk(gk_t);
  const Tensor Aqk_t = to<Tensor>(stack[5]);
  const TensorMeta Aqk_meta = meta_of(Aqk_t);
  const shim::Tensor Aqk(Aqk_t);
  const Tensor Akk_t = to<Tensor>(stack[6]);
  const TensorMeta Akk_meta = meta_of(Akk_t);
  const shim::Tensor Akk(Akk_t);
  const auto w_t = to<std::optional<Tensor>>(stack[7]);
  const TensorMeta w_meta = w_t.has_value() ? meta_of(*w_t) : TensorMeta();
  const std::optional<shim::Tensor> w = w_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*w_t)) : std::nullopt;
  const auto qg_t = to<std::optional<Tensor>>(stack[8]);
  const TensorMeta qg_meta = qg_t.has_value() ? meta_of(*qg_t) : TensorMeta();
  const std::optional<shim::Tensor> qg = qg_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*qg_t)) : std::nullopt;
  const auto kg_t = to<std::optional<Tensor>>(stack[9]);
  const TensorMeta kg_meta = kg_t.has_value() ? meta_of(*kg_t) : TensorMeta();
  const std::optional<shim::Tensor> kg = kg_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*kg_t)) : std::nullopt;
  const auto v_new_t = to<std::optional<Tensor>>(stack[10]);
  const TensorMeta v_new_meta = v_new_t.has_value() ? meta_of(*v_new_t) : TensorMeta();
  const std::optional<shim::Tensor> v_new = v_new_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*v_new_t)) : std::nullopt;
  const auto h_t = to<std::optional<Tensor>>(stack[11]);
  const TensorMeta h_meta = h_t.has_value() ? meta_of(*h_t) : TensorMeta();
  const std::optional<shim::Tensor> h = h_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*h_t)) : std::nullopt;
  const Tensor d_o_t = to<Tensor>(stack[12]);
  const TensorMeta d_o_meta = meta_of(d_o_t);
  const shim::Tensor d_o(d_o_t);
  const auto raw_g_t = to<std::optional<Tensor>>(stack[13]);
  const TensorMeta raw_g_meta = raw_g_t.has_value() ? meta_of(*raw_g_t) : TensorMeta();
  const std::optional<shim::Tensor> raw_g = raw_g_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*raw_g_t)) : std::nullopt;
  const auto A_log_t = to<std::optional<Tensor>>(stack[14]);
  const TensorMeta A_log_meta = A_log_t.has_value() ? meta_of(*A_log_t) : TensorMeta();
  const std::optional<shim::Tensor> A_log = A_log_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*A_log_t)) : std::nullopt;
  const auto dt_bias_t = to<std::optional<Tensor>>(stack[15]);
  const TensorMeta dt_bias_meta = dt_bias_t.has_value() ? meta_of(*dt_bias_t) : TensorMeta();
  const std::optional<shim::Tensor> dt_bias = dt_bias_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*dt_bias_t)) : std::nullopt;
  const auto initial_state_t = to<std::optional<Tensor>>(stack[16]);
  const TensorMeta initial_state_meta = initial_state_t.has_value() ? meta_of(*initial_state_t) : TensorMeta();
  const std::optional<shim::Tensor> initial_state = initial_state_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*initial_state_t)) : std::nullopt;
  const auto dht_t = to<std::optional<Tensor>>(stack[17]);
  const TensorMeta dht_meta = dht_t.has_value() ? meta_of(*dht_t) : TensorMeta();
  const std::optional<shim::Tensor> dht = dht_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*dht_t)) : std::nullopt;
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[18]);
  const std::vector<int64_t> cu_seqlens = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[19]);
  const std::vector<int64_t> chunk_indices = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices);
  const double scale = to<double>(stack[20]);
  const int64_t chunk_size = to<int64_t>(stack[21]);
  const bool safe_gate = to<bool>(stack[22]);
  const bool use_gate_in_kernel = to<bool>(stack[23]);
  const double lower_bound = to<double>(stack[24]);
  const bool disable_recompute = to<bool>(stack[25]);
  const bool use_exp2 = to<bool>(stack[26]);
  const bool state_v_first = to<bool>(stack[27]);
  const int64_t stream = to<int64_t>(stack[28]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_chunk_kda_bwd_GetWorkspaceFn>(
      rt.symbol("aclnnChunkKdaBwdGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnChunkKdaBwd"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<shim::Tensor> outputs;
  outputs.push_back(shim::empty({q.sizes()}, q.options().dtype(shim::kFloat)));
  outputs.push_back(shim::empty({k.sizes()}, k.options().dtype(shim::kFloat)));
  outputs.push_back(shim::empty_like(v));
  outputs.push_back(shim::empty({beta.sizes()}, beta.options().dtype(shim::kFloat)));
  outputs.push_back(shim::empty_like(gk));
  outputs.push_back(shim::Tensor());
  outputs.push_back(shim::Tensor());
  outputs.push_back(shim::Tensor());

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(q_meta));
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(v_meta));
  views.push_back(std::make_unique<AclTensorView>(beta_meta));
  views.push_back(std::make_unique<AclTensorView>(gk_meta));
  views.push_back(std::make_unique<AclTensorView>(Aqk_meta));
  views.push_back(std::make_unique<AclTensorView>(Akk_meta));
  views.push_back(std::make_unique<AclTensorView>(w_meta));
  views.push_back(std::make_unique<AclTensorView>(qg_meta));
  views.push_back(std::make_unique<AclTensorView>(kg_meta));
  views.push_back(std::make_unique<AclTensorView>(v_new_meta));
  views.push_back(std::make_unique<AclTensorView>(h_meta));
  views.push_back(std::make_unique<AclTensorView>(d_o_meta));
  views.push_back(std::make_unique<AclTensorView>(raw_g_meta));
  views.push_back(std::make_unique<AclTensorView>(A_log_meta));
  views.push_back(std::make_unique<AclTensorView>(dt_bias_meta));
  views.push_back(std::make_unique<AclTensorView>(initial_state_meta));
  views.push_back(std::make_unique<AclTensorView>(dht_meta));
  views.push_back(std::make_unique<AclTensorView>(outputs[0].defined() ? meta_of(outputs[0].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[1].defined() ? meta_of(outputs[1].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[2].defined() ? meta_of(outputs[2].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[3].defined() ? meta_of(outputs[3].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[4].defined() ? meta_of(outputs[4].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[5].defined() ? meta_of(outputs[5].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[6].defined() ? meta_of(outputs[6].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[7].defined() ? meta_of(outputs[7].tensor()) : TensorMeta()));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      views[6]->get(),
      views[7]->get(),
      views[8]->get(),
      views[9]->get(),
      views[10]->get(),
      views[11]->get(),
      views[12]->get(),
      views[13]->get(),
      views[14]->get(),
      views[15]->get(),
      views[16]->get(),
      views[17]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      scale,
      chunk_size,
      safe_gate,
      use_gate_in_kernel,
      lower_bound,
      disable_recompute,
      use_exp2,
      state_v_first,
      views[18]->get(),
      views[19]->get(),
      views[20]->get(),
      views[21]->get(),
      views[22]->get(),
      views[23]->get(),
      views[24]->get(),
      views[25]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkKdaBwdGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   q_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkKdaBwd failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0].tensor());
  stack[1] = from(outputs[1].tensor());
  stack[2] = from(outputs[2].tensor());
  stack[3] = from(outputs[3].tensor());
  stack[4] = from(outputs[4].tensor());
  stack[5] = outputs[5].defined() ? from(std::optional<Tensor>(outputs[5].tensor())) : from(std::nullopt);
  stack[6] = outputs[6].defined() ? from(std::optional<Tensor>(outputs[6].tensor())) : from(std::nullopt);
  stack[7] = outputs[7].defined() ? from(std::optional<Tensor>(outputs[7].tensor())) : from(std::nullopt);
}

// ---- generated: npu_chunk_kda_bwd_intra ----
inline const char* npu_chunk_kda_bwd_intra_layout_name(int64_t code) {
  switch (code) {
    case 0: return "BSND";
    case 1: return "BNSD";
    default:
      throw std::runtime_error(
          "npu_chunk_kda_bwd_intra: bad layout code " + std::to_string(code));
  }
}
using npu_chunk_kda_bwd_intra_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    int64_t,
    bool,
    const char*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_chunk_kda_bwd_intra =
    "npu_chunk_kda_bwd_intra(Tensor q, Tensor k, Tensor gk, Tensor beta, Tensor dAqk, Tensor dAkk, Tensor dq, Tensor dk, Tensor db, Tensor dg, Tensor? cu_seqlens, Tensor? chunk_indices, int chunk_size, bool safe_gate, int layout, int stream) -> (Tensor, Tensor, Tensor, Tensor)";

void boxed_npu_chunk_kda_bwd_intra(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor q_t = to<Tensor>(stack[0]);
  const TensorMeta q_meta = meta_of(q_t);
  const Tensor k_t = to<Tensor>(stack[1]);
  const TensorMeta k_meta = meta_of(k_t);
  const Tensor gk_t = to<Tensor>(stack[2]);
  const TensorMeta gk_meta = meta_of(gk_t);
  const Tensor beta_t = to<Tensor>(stack[3]);
  const TensorMeta beta_meta = meta_of(beta_t);
  const Tensor dAqk_t = to<Tensor>(stack[4]);
  const TensorMeta dAqk_meta = meta_of(dAqk_t);
  const Tensor dAkk_t = to<Tensor>(stack[5]);
  const TensorMeta dAkk_meta = meta_of(dAkk_t);
  const Tensor dq_t = to<Tensor>(stack[6]);
  const TensorMeta dq_meta = meta_of(dq_t);
  const Tensor dk_t = to<Tensor>(stack[7]);
  const TensorMeta dk_meta = meta_of(dk_t);
  const Tensor db_t = to<Tensor>(stack[8]);
  const TensorMeta db_meta = meta_of(db_t);
  const Tensor dg_t = to<Tensor>(stack[9]);
  const TensorMeta dg_meta = meta_of(dg_t);
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[10]);
  const std::vector<int64_t> cu_seqlens_values = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens_values);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[11]);
  const std::vector<int64_t> chunk_indices_values = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices_values);
  const int64_t chunk_size = to<int64_t>(stack[12]);
  const bool safe_gate = to<bool>(stack[13]);
  const int64_t layout = to<int64_t>(stack[14]);
  const int64_t stream = to<int64_t>(stack[15]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_chunk_kda_bwd_intra_GetWorkspaceFn>(
      rt.symbol("aclnnChunkKdaBwdIntraGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnChunkKdaBwdIntra"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<Tensor> outputs;
  std::vector<bool> output_present;
  outputs.push_back(allocate_like(dq_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(dk_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(db_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(dg_meta));
  output_present.push_back(true);

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(q_meta));
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(gk_meta));
  views.push_back(std::make_unique<AclTensorView>(beta_meta));
  views.push_back(std::make_unique<AclTensorView>(dAqk_meta));
  views.push_back(std::make_unique<AclTensorView>(dAkk_meta));
  views.push_back(std::make_unique<AclTensorView>(dq_meta));
  views.push_back(std::make_unique<AclTensorView>(dk_meta));
  views.push_back(std::make_unique<AclTensorView>(db_meta));
  views.push_back(std::make_unique<AclTensorView>(dg_meta));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[0])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[1])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[2])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[3])));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      views[6]->get(),
      views[7]->get(),
      views[8]->get(),
      views[9]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      chunk_size,
      safe_gate,
      npu_chunk_kda_bwd_intra_layout_name(layout),
      views[10]->get(),
      views[11]->get(),
      views[12]->get(),
      views[13]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkKdaBwdIntraGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   q_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkKdaBwdIntra failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0]);
  stack[1] = from(outputs[1]);
  stack[2] = from(outputs[2]);
  stack[3] = from(outputs[3]);
}

// ---- generated: npu_chunk_kda_fwd ----
inline const char* npu_chunk_kda_fwd_layout_name(int64_t code) {
  switch (code) {
    case 0: return "BSND";
    case 1: return "BNSD";
    case 2: return "TND";
    case 3: return "NTD";
    default:
      throw std::runtime_error(
          "npu_chunk_kda_fwd: bad layout code " + std::to_string(code));
  }
}
using npu_chunk_kda_fwd_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    const char*,
    double,
    int64_t,
    bool,
    double,
    bool,
    bool,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_chunk_kda_fwd =
    "npu_chunk_kda_fwd(Tensor q, Tensor k, Tensor v, Tensor g, Tensor beta, Tensor? A_log, Tensor? dt_bias, Tensor? initial_state, Tensor? cu_seqlens, Tensor? chunk_indices, int layout, float scale, int chunk_size, bool safe_gate, float lower_bound, bool use_gate_in_kernel, bool state_v_first, bool output_final_state, bool disable_recompute, bool return_intermediate_states, int stream) -> (Tensor, Tensor?, Tensor?, Tensor, Tensor, Tensor?, Tensor?, Tensor?, Tensor?, Tensor?, Tensor?)";

void boxed_npu_chunk_kda_fwd(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor q_t = to<Tensor>(stack[0]);
  const TensorMeta q_meta = meta_of(q_t);
  const shim::Tensor q(q_t);
  const Tensor k_t = to<Tensor>(stack[1]);
  const TensorMeta k_meta = meta_of(k_t);
  const shim::Tensor k(k_t);
  const Tensor v_t = to<Tensor>(stack[2]);
  const TensorMeta v_meta = meta_of(v_t);
  const shim::Tensor v(v_t);
  const Tensor g_t = to<Tensor>(stack[3]);
  const TensorMeta g_meta = meta_of(g_t);
  const shim::Tensor g(g_t);
  const Tensor beta_t = to<Tensor>(stack[4]);
  const TensorMeta beta_meta = meta_of(beta_t);
  const shim::Tensor beta(beta_t);
  const auto A_log_t = to<std::optional<Tensor>>(stack[5]);
  const TensorMeta A_log_meta = A_log_t.has_value() ? meta_of(*A_log_t) : TensorMeta();
  const std::optional<shim::Tensor> A_log = A_log_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*A_log_t)) : std::nullopt;
  const auto dt_bias_t = to<std::optional<Tensor>>(stack[6]);
  const TensorMeta dt_bias_meta = dt_bias_t.has_value() ? meta_of(*dt_bias_t) : TensorMeta();
  const std::optional<shim::Tensor> dt_bias = dt_bias_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*dt_bias_t)) : std::nullopt;
  const auto initial_state_t = to<std::optional<Tensor>>(stack[7]);
  const TensorMeta initial_state_meta = initial_state_t.has_value() ? meta_of(*initial_state_t) : TensorMeta();
  const std::optional<shim::Tensor> initial_state = initial_state_t.has_value() ? std::optional<shim::Tensor>(shim::Tensor(*initial_state_t)) : std::nullopt;
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[8]);
  const std::vector<int64_t> cu_seqlens = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[9]);
  const std::vector<int64_t> chunk_indices = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices);
  const int64_t layout_code = to<int64_t>(stack[10]);
  const std::string layout = npu_chunk_kda_fwd_layout_name(layout_code);
  const double scale = to<double>(stack[11]);
  const int64_t chunk_size = to<int64_t>(stack[12]);
  const bool safe_gate = to<bool>(stack[13]);
  const double lower_bound = to<double>(stack[14]);
  const bool use_gate_in_kernel = to<bool>(stack[15]);
  const bool state_v_first = to<bool>(stack[16]);
  const bool output_final_state = to<bool>(stack[17]);
  const bool disable_recompute = to<bool>(stack[18]);
  const bool return_intermediate_states = to<bool>(stack[19]);
  const int64_t stream = to<int64_t>(stack[20]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_chunk_kda_fwd_GetWorkspaceFn>(
      rt.symbol("aclnnChunkKdaFwdGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnChunkKdaFwd"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<shim::Tensor> outputs;
  outputs.push_back(shim::empty(kda_fwd_attn_sizes(q, v, layout), q.options()));
  if (output_final_state) {
    outputs.push_back(shim::empty(kda_fwd_state_sizes(q, v, layout, cu_seqlens, state_v_first), q.options().dtype(shim::kFloat)));
  } else {
    outputs.push_back(shim::Tensor());
  }
  if (!use_gate_in_kernel || disable_recompute) {
    outputs.push_back(shim::empty(kda_fwd_khead_sizes(q, v, layout), q.options().dtype(shim::kFloat)));
  } else {
    outputs.push_back(shim::Tensor());
  }
  outputs.push_back(shim::empty(kda_fwd_matrix_sizes(q, v, layout, chunk_size), q.options()));
  outputs.push_back(shim::empty(kda_fwd_matrix_sizes(q, v, layout, chunk_size), q.options()));
  if (disable_recompute) {
    outputs.push_back(shim::empty(kda_fwd_khead_sizes(q, v, layout), q.options()));
  } else {
    outputs.push_back(shim::Tensor());
  }
  if (disable_recompute) {
    outputs.push_back(shim::empty(kda_fwd_vhead_sizes(q, v, layout), q.options()));
  } else {
    outputs.push_back(shim::Tensor());
  }
  if (disable_recompute) {
    outputs.push_back(shim::empty(kda_fwd_khead_sizes(q, v, layout), q.options()));
  } else {
    outputs.push_back(shim::Tensor());
  }
  if (disable_recompute) {
    outputs.push_back(shim::empty(kda_fwd_khead_sizes(q, v, layout), q.options()));
  } else {
    outputs.push_back(shim::Tensor());
  }
  if (disable_recompute) {
    outputs.push_back(shim::empty(kda_fwd_vhead_sizes(q, v, layout), q.options()));
  } else {
    outputs.push_back(shim::Tensor());
  }
  if (disable_recompute || return_intermediate_states) {
    outputs.push_back(shim::empty(kda_fwd_h_sizes(q, v, layout, cu_seqlens, chunk_indices, chunk_size, state_v_first), q.options()));
  } else {
    outputs.push_back(shim::Tensor());
  }

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(q_meta));
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(v_meta));
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(beta_meta));
  views.push_back(std::make_unique<AclTensorView>(A_log_meta));
  views.push_back(std::make_unique<AclTensorView>(dt_bias_meta));
  views.push_back(std::make_unique<AclTensorView>(initial_state_meta));
  views.push_back(std::make_unique<AclTensorView>(outputs[0].defined() ? meta_of(outputs[0].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[1].defined() ? meta_of(outputs[1].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[2].defined() ? meta_of(outputs[2].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[3].defined() ? meta_of(outputs[3].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[4].defined() ? meta_of(outputs[4].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[5].defined() ? meta_of(outputs[5].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[6].defined() ? meta_of(outputs[6].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[7].defined() ? meta_of(outputs[7].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[8].defined() ? meta_of(outputs[8].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[9].defined() ? meta_of(outputs[9].tensor()) : TensorMeta()));
  views.push_back(std::make_unique<AclTensorView>(outputs[10].defined() ? meta_of(outputs[10].tensor()) : TensorMeta()));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      views[6]->get(),
      views[7]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      layout.c_str(),
      scale,
      chunk_size,
      safe_gate,
      lower_bound,
      use_gate_in_kernel,
      state_v_first,
      views[8]->get(),
      views[9]->get(),
      views[10]->get(),
      views[11]->get(),
      views[12]->get(),
      views[13]->get(),
      views[14]->get(),
      views[15]->get(),
      views[16]->get(),
      views[17]->get(),
      views[18]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkKdaFwdGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   q_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkKdaFwd failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0].tensor());
  stack[1] = outputs[1].defined() ? from(std::optional<Tensor>(outputs[1].tensor())) : from(std::nullopt);
  stack[2] = outputs[2].defined() ? from(std::optional<Tensor>(outputs[2].tensor())) : from(std::nullopt);
  stack[3] = from(outputs[3].tensor());
  stack[4] = from(outputs[4].tensor());
  stack[5] = outputs[5].defined() ? from(std::optional<Tensor>(outputs[5].tensor())) : from(std::nullopt);
  stack[6] = outputs[6].defined() ? from(std::optional<Tensor>(outputs[6].tensor())) : from(std::nullopt);
  stack[7] = outputs[7].defined() ? from(std::optional<Tensor>(outputs[7].tensor())) : from(std::nullopt);
  stack[8] = outputs[8].defined() ? from(std::optional<Tensor>(outputs[8].tensor())) : from(std::nullopt);
  stack[9] = outputs[9].defined() ? from(std::optional<Tensor>(outputs[9].tensor())) : from(std::nullopt);
  stack[10] = outputs[10].defined() ? from(std::optional<Tensor>(outputs[10].tensor())) : from(std::nullopt);
}

// ---- generated: npu_chunk_local_cumsum ----
inline const char* npu_chunk_local_cumsum_output_dtype_name(int64_t code) {
  switch (code) {
    case 0: return "float32";
    case 1: return "bfloat16";
    default:
      throw std::runtime_error(
          "npu_chunk_local_cumsum: bad output_dtype code " + std::to_string(code));
  }
}
using npu_chunk_local_cumsum_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    int64_t,
    bool,
    double,
    bool,
    const char*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_chunk_local_cumsum =
    "npu_chunk_local_cumsum(Tensor g, Tensor? cu_seqlens, Tensor? chunk_indices, int chunk_size, bool reverse, float scale, bool head_first, int output_dtype, int stream) -> Tensor";

void boxed_npu_chunk_local_cumsum(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor g_t = to<Tensor>(stack[0]);
  const TensorMeta g_meta = meta_of(g_t);
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[1]);
  const std::vector<int64_t> cu_seqlens_values = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens_values);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[2]);
  const std::vector<int64_t> chunk_indices_values = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices_values);
  const int64_t chunk_size = to<int64_t>(stack[3]);
  const bool reverse = to<bool>(stack[4]);
  const double scale = to<double>(stack[5]);
  const bool head_first = to<bool>(stack[6]);
  const int64_t output_dtype = to<int64_t>(stack[7]);
  const int64_t stream = to<int64_t>(stack[8]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_chunk_local_cumsum_GetWorkspaceFn>(
      rt.symbol("aclnnChunkLocalCumsumGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnChunkLocalCumsum"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<Tensor> outputs;
  std::vector<bool> output_present;
  outputs.push_back(allocate_sizes({g_meta.sizes}, (output_dtype == 0 ? 6 : 15), g_meta));
  output_present.push_back(true);

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[0])));

  const int get_ret = get_ws(
      views[0]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      chunk_size,
      reverse,
      scale,
      head_first,
      npu_chunk_local_cumsum_output_dtype_name(output_dtype),
      views[1]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkLocalCumsumGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   g_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkLocalCumsum failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0]);
}

// ---- generated: npu_chunk_scaled_dot_kkt ----
using npu_chunk_scaled_dot_kkt_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    int64_t,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_chunk_scaled_dot_kkt =
    "npu_chunk_scaled_dot_kkt(Tensor k, Tensor g, Tensor beta, Tensor? cu_seqlens, Tensor? chunk_indices, int chunk_size, int stream) -> Tensor";

void boxed_npu_chunk_scaled_dot_kkt(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor k_t = to<Tensor>(stack[0]);
  const TensorMeta k_meta = meta_of(k_t);
  const Tensor g_t = to<Tensor>(stack[1]);
  const TensorMeta g_meta = meta_of(g_t);
  const Tensor beta_t = to<Tensor>(stack[2]);
  const TensorMeta beta_meta = meta_of(beta_t);
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[3]);
  const std::vector<int64_t> cu_seqlens_values = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens_values);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[4]);
  const std::vector<int64_t> chunk_indices_values = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices_values);
  const int64_t chunk_size = to<int64_t>(stack[5]);
  const int64_t stream = to<int64_t>(stack[6]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_chunk_scaled_dot_kkt_GetWorkspaceFn>(
      rt.symbol("aclnnChunkScaledDotKktGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnChunkScaledDotKkt"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<Tensor> outputs;
  std::vector<bool> output_present;
  outputs.push_back(allocate_sizes({size_of(k_meta, 0), size_of(g_meta, 1), size_of(k_meta, 2), chunk_size}, 6, k_meta));
  output_present.push_back(true);

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(beta_meta));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[0])));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      chunk_size,
      views[3]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkScaledDotKktGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   k_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnChunkScaledDotKkt failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0]);
}

// ---- generated: npu_fast_gelu_custom ----
using npu_fast_gelu_custom_GetWorkspaceFn = int (*)(
    const aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_fast_gelu_custom =
    "npu_fast_gelu_custom(Tensor self, int stream) -> Tensor";

void boxed_npu_fast_gelu_custom(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor self_t = to<Tensor>(stack[0]);
  const TensorMeta self_meta = meta_of(self_t);
  const int64_t stream = to<int64_t>(stack[1]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_fast_gelu_custom_GetWorkspaceFn>(
      rt.symbol("aclnnFastGeluGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnFastGelu"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<Tensor> outputs;
  std::vector<bool> output_present;
  outputs.push_back(allocate_like(self_meta));
  output_present.push_back(true);

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(self_meta));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[0])));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnFastGeluGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   self_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnFastGelu failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0]);
}

// ---- generated: npu_fast_gelu_custom_backward ----
using npu_fast_gelu_custom_backward_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_fast_gelu_custom_backward =
    "npu_fast_gelu_custom_backward(Tensor grad, Tensor self, int stream) -> Tensor";

void boxed_npu_fast_gelu_custom_backward(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor grad_t = to<Tensor>(stack[0]);
  const TensorMeta grad_meta = meta_of(grad_t);
  const Tensor self_t = to<Tensor>(stack[1]);
  const TensorMeta self_meta = meta_of(self_t);
  const int64_t stream = to<int64_t>(stack[2]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_fast_gelu_custom_backward_GetWorkspaceFn>(
      rt.symbol("aclnnFastGeluBackwardGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnFastGeluBackward"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<Tensor> outputs;
  std::vector<bool> output_present;
  outputs.push_back(allocate_like(grad_meta));
  output_present.push_back(true);

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(grad_meta));
  views.push_back(std::make_unique<AclTensorView>(self_meta));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[0])));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnFastGeluBackwardGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   grad_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnFastGeluBackward failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0]);
}

// ---- generated: npu_kda_gate_cumsum ----
using npu_kda_gate_cumsum_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    int64_t,
    bool,
    bool,
    double,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_kda_gate_cumsum =
    "npu_kda_gate_cumsum(Tensor g, Tensor? A_log, Tensor? dt_bias, Tensor? cu_seqlens, int chunk_size, bool use_gate_in_kernel, bool safe_gate, float lower_bound, int stream) -> Tensor";

void boxed_npu_kda_gate_cumsum(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor g_t = to<Tensor>(stack[0]);
  const TensorMeta g_meta = meta_of(g_t);
  const auto A_log_t = to<std::optional<Tensor>>(stack[1]);
  const TensorMeta A_log_meta = A_log_t.has_value() ? meta_of(*A_log_t) : TensorMeta();
  const auto dt_bias_t = to<std::optional<Tensor>>(stack[2]);
  const TensorMeta dt_bias_meta = dt_bias_t.has_value() ? meta_of(*dt_bias_t) : TensorMeta();
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[3]);
  const std::vector<int64_t> cu_seqlens_values = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens_values);
  const int64_t chunk_size = to<int64_t>(stack[4]);
  const bool use_gate_in_kernel = to<bool>(stack[5]);
  const bool safe_gate = to<bool>(stack[6]);
  const double lower_bound = to<double>(stack[7]);
  const int64_t stream = to<int64_t>(stack[8]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_kda_gate_cumsum_GetWorkspaceFn>(
      rt.symbol("aclnnKdaGateCumsumGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnKdaGateCumsum"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<Tensor> outputs;
  std::vector<bool> output_present;
  outputs.push_back(allocate_sizes({g_meta.sizes}, 6, g_meta));
  output_present.push_back(true);

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(A_log_meta));
  views.push_back(std::make_unique<AclTensorView>(dt_bias_meta));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[0])));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      cu_seqlens_view.get(),
      chunk_size,
      use_gate_in_kernel,
      safe_gate,
      lower_bound,
      views[3]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnKdaGateCumsumGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   g_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnKdaGateCumsum failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0]);
}

// ---- generated: npu_prepare_wy_repr_bwd ----
using npu_prepare_wy_repr_bwd_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    int64_t,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_prepare_wy_repr_bwd =
    "npu_prepare_wy_repr_bwd(Tensor k, Tensor v, Tensor beta, Tensor A, Tensor dw, Tensor du, Tensor g, Tensor? cu_seqlens, Tensor? chunk_indices, int chunk_size, int stream) -> (Tensor, Tensor, Tensor, Tensor)";

void boxed_npu_prepare_wy_repr_bwd(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor k_t = to<Tensor>(stack[0]);
  const TensorMeta k_meta = meta_of(k_t);
  const Tensor v_t = to<Tensor>(stack[1]);
  const TensorMeta v_meta = meta_of(v_t);
  const Tensor beta_t = to<Tensor>(stack[2]);
  const TensorMeta beta_meta = meta_of(beta_t);
  const Tensor A_t = to<Tensor>(stack[3]);
  const TensorMeta A_meta = meta_of(A_t);
  const Tensor dw_t = to<Tensor>(stack[4]);
  const TensorMeta dw_meta = meta_of(dw_t);
  const Tensor du_t = to<Tensor>(stack[5]);
  const TensorMeta du_meta = meta_of(du_t);
  const Tensor g_t = to<Tensor>(stack[6]);
  const TensorMeta g_meta = meta_of(g_t);
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[7]);
  const std::vector<int64_t> cu_seqlens_values = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens_values);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[8]);
  const std::vector<int64_t> chunk_indices_values = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices_values);
  const int64_t chunk_size = to<int64_t>(stack[9]);
  const int64_t stream = to<int64_t>(stack[10]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_prepare_wy_repr_bwd_GetWorkspaceFn>(
      rt.symbol("aclnnPrepareWyReprBwdGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnPrepareWyReprBwd"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<Tensor> outputs;
  std::vector<bool> output_present;
  outputs.push_back(allocate_like(k_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(v_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(beta_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(g_meta));
  output_present.push_back(true);

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(v_meta));
  views.push_back(std::make_unique<AclTensorView>(beta_meta));
  views.push_back(std::make_unique<AclTensorView>(A_meta));
  views.push_back(std::make_unique<AclTensorView>(dw_meta));
  views.push_back(std::make_unique<AclTensorView>(du_meta));
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[0])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[1])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[2])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[3])));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      views[6]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      chunk_size,
      views[7]->get(),
      views[8]->get(),
      views[9]->get(),
      views[10]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnPrepareWyReprBwdGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   k_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnPrepareWyReprBwd failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0]);
  stack[1] = from(outputs[1]);
  stack[2] = from(outputs[2]);
  stack[3] = from(outputs[3]);
}

// ---- generated: npu_prepare_wy_repr_bwd_da ----
using npu_prepare_wy_repr_bwd_da_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    int64_t,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_prepare_wy_repr_bwd_da =
    "npu_prepare_wy_repr_bwd_da(Tensor k, Tensor v, Tensor beta, Tensor A, Tensor dw, Tensor du, Tensor g, Tensor? cu_seqlens, Tensor? chunk_indices, int chunk_size, int stream) -> Tensor";

void boxed_npu_prepare_wy_repr_bwd_da(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor k_t = to<Tensor>(stack[0]);
  const TensorMeta k_meta = meta_of(k_t);
  const Tensor v_t = to<Tensor>(stack[1]);
  const TensorMeta v_meta = meta_of(v_t);
  const Tensor beta_t = to<Tensor>(stack[2]);
  const TensorMeta beta_meta = meta_of(beta_t);
  const Tensor A_t = to<Tensor>(stack[3]);
  const TensorMeta A_meta = meta_of(A_t);
  const Tensor dw_t = to<Tensor>(stack[4]);
  const TensorMeta dw_meta = meta_of(dw_t);
  const Tensor du_t = to<Tensor>(stack[5]);
  const TensorMeta du_meta = meta_of(du_t);
  const Tensor g_t = to<Tensor>(stack[6]);
  const TensorMeta g_meta = meta_of(g_t);
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[7]);
  const std::vector<int64_t> cu_seqlens_values = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens_values);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[8]);
  const std::vector<int64_t> chunk_indices_values = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices_values);
  const int64_t chunk_size = to<int64_t>(stack[9]);
  const int64_t stream = to<int64_t>(stack[10]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_prepare_wy_repr_bwd_da_GetWorkspaceFn>(
      rt.symbol("aclnnPrepareWyReprBwdDaGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnPrepareWyReprBwdDa"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<Tensor> outputs;
  std::vector<bool> output_present;
  outputs.push_back(allocate_like(A_meta));
  output_present.push_back(true);

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(v_meta));
  views.push_back(std::make_unique<AclTensorView>(beta_meta));
  views.push_back(std::make_unique<AclTensorView>(A_meta));
  views.push_back(std::make_unique<AclTensorView>(dw_meta));
  views.push_back(std::make_unique<AclTensorView>(du_meta));
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[0])));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      views[6]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      chunk_size,
      views[7]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnPrepareWyReprBwdDaGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   k_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnPrepareWyReprBwdDa failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0]);
}

// ---- generated: npu_prepare_wy_repr_bwd_full ----
using npu_prepare_wy_repr_bwd_full_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    int64_t,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_prepare_wy_repr_bwd_full =
    "npu_prepare_wy_repr_bwd_full(Tensor k, Tensor v, Tensor beta, Tensor A, Tensor dA, Tensor dw, Tensor du, Tensor g, Tensor? cu_seqlens, Tensor? chunk_indices, int chunk_size, int stream) -> (Tensor, Tensor, Tensor, Tensor)";

void boxed_npu_prepare_wy_repr_bwd_full(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor k_t = to<Tensor>(stack[0]);
  const TensorMeta k_meta = meta_of(k_t);
  const Tensor v_t = to<Tensor>(stack[1]);
  const TensorMeta v_meta = meta_of(v_t);
  const Tensor beta_t = to<Tensor>(stack[2]);
  const TensorMeta beta_meta = meta_of(beta_t);
  const Tensor A_t = to<Tensor>(stack[3]);
  const TensorMeta A_meta = meta_of(A_t);
  const Tensor dA_t = to<Tensor>(stack[4]);
  const TensorMeta dA_meta = meta_of(dA_t);
  const Tensor dw_t = to<Tensor>(stack[5]);
  const TensorMeta dw_meta = meta_of(dw_t);
  const Tensor du_t = to<Tensor>(stack[6]);
  const TensorMeta du_meta = meta_of(du_t);
  const Tensor g_t = to<Tensor>(stack[7]);
  const TensorMeta g_meta = meta_of(g_t);
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[8]);
  const std::vector<int64_t> cu_seqlens_values = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens_values);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[9]);
  const std::vector<int64_t> chunk_indices_values = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices_values);
  const int64_t chunk_size = to<int64_t>(stack[10]);
  const int64_t stream = to<int64_t>(stack[11]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_prepare_wy_repr_bwd_full_GetWorkspaceFn>(
      rt.symbol("aclnnPrepareWyReprBwdFullGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnPrepareWyReprBwdFull"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<Tensor> outputs;
  std::vector<bool> output_present;
  outputs.push_back(allocate_like(k_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(v_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(beta_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(g_meta));
  output_present.push_back(true);

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(v_meta));
  views.push_back(std::make_unique<AclTensorView>(beta_meta));
  views.push_back(std::make_unique<AclTensorView>(A_meta));
  views.push_back(std::make_unique<AclTensorView>(dA_meta));
  views.push_back(std::make_unique<AclTensorView>(dw_meta));
  views.push_back(std::make_unique<AclTensorView>(du_meta));
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[0])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[1])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[2])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[3])));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      views[6]->get(),
      views[7]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      chunk_size,
      views[8]->get(),
      views[9]->get(),
      views[10]->get(),
      views[11]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnPrepareWyReprBwdFullGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   k_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnPrepareWyReprBwdFull failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0]);
  stack[1] = from(outputs[1]);
  stack[2] = from(outputs[2]);
  stack[3] = from(outputs[3]);
}

// ---- generated: npu_recompute_w_u_fwd ----
using npu_recompute_w_u_fwd_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    int64_t,
    aclTensor*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_recompute_w_u_fwd =
    "npu_recompute_w_u_fwd(Tensor k, Tensor v, Tensor beta, Tensor A, Tensor? g, Tensor? gk, Tensor? cu_seqlens, Tensor? chunk_indices, int chunk_size, int stream) -> (Tensor, Tensor)";

void boxed_npu_recompute_w_u_fwd(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor k_t = to<Tensor>(stack[0]);
  const TensorMeta k_meta = meta_of(k_t);
  const Tensor v_t = to<Tensor>(stack[1]);
  const TensorMeta v_meta = meta_of(v_t);
  const Tensor beta_t = to<Tensor>(stack[2]);
  const TensorMeta beta_meta = meta_of(beta_t);
  const Tensor A_t = to<Tensor>(stack[3]);
  const TensorMeta A_meta = meta_of(A_t);
  const auto g_t = to<std::optional<Tensor>>(stack[4]);
  const TensorMeta g_meta = g_t.has_value() ? meta_of(*g_t) : TensorMeta();
  const auto gk_t = to<std::optional<Tensor>>(stack[5]);
  const TensorMeta gk_meta = gk_t.has_value() ? meta_of(*gk_t) : TensorMeta();
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[6]);
  const std::vector<int64_t> cu_seqlens_values = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens_values);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[7]);
  const std::vector<int64_t> chunk_indices_values = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices_values);
  const int64_t chunk_size = to<int64_t>(stack[8]);
  const int64_t stream = to<int64_t>(stack[9]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_recompute_w_u_fwd_GetWorkspaceFn>(
      rt.symbol("aclnnRecomputeWUFwdGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnRecomputeWUFwd"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<Tensor> outputs;
  std::vector<bool> output_present;
  outputs.push_back(allocate_sizes({size_of(v_meta, 0), size_of(v_meta, 1), size_of(v_meta, 2), size_of(k_meta, 3)}, k_meta.scalar_type, k_meta));
  output_present.push_back(true);
  outputs.push_back(allocate_like(v_meta));
  output_present.push_back(true);

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(k_meta));
  views.push_back(std::make_unique<AclTensorView>(v_meta));
  views.push_back(std::make_unique<AclTensorView>(beta_meta));
  views.push_back(std::make_unique<AclTensorView>(A_meta));
  views.push_back(std::make_unique<AclTensorView>(g_meta));
  views.push_back(std::make_unique<AclTensorView>(gk_meta));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[0])));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[1])));

  const int get_ret = get_ws(
      views[0]->get(),
      views[1]->get(),
      views[2]->get(),
      views[3]->get(),
      views[4]->get(),
      views[5]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      chunk_size,
      views[6]->get(),
      views[7]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnRecomputeWUFwdGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   k_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnRecomputeWUFwd failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0]);
  stack[1] = from(outputs[1]);
}

// ---- generated: npu_solve_tri ----
inline const char* npu_solve_tri_layout_name(int64_t code) {
  switch (code) {
    case 0: return "bsnd";
    case 1: return "bnsd";
    case 2: return "tnd";
    case 3: return "ntd";
    default:
      throw std::runtime_error(
          "npu_solve_tri: bad layout code " + std::to_string(code));
  }
}
using npu_solve_tri_GetWorkspaceFn = int (*)(
    const aclTensor*,
    const aclIntArray*,
    const aclIntArray*,
    const char*,
    aclTensor*,
    uint64_t*, aclOpExecutor**);
constexpr const char* kSchema_npu_solve_tri =
    "npu_solve_tri(Tensor x, Tensor? cu_seqlens, Tensor? chunk_indices, int layout, int stream) -> Tensor";

void boxed_npu_solve_tri(StableIValue* stack, uint64_t num_inputs,
                 uint64_t num_outputs) {
  (void)num_inputs;
  (void)num_outputs;
  const Tensor x_t = to<Tensor>(stack[0]);
  const TensorMeta x_meta = meta_of(x_t);
  const auto cu_seqlens_t = to<std::optional<Tensor>>(stack[1]);
  const std::vector<int64_t> cu_seqlens_values = cu_seqlens_t.has_value() ? host_int_values(cu_seqlens_t->get()) : std::vector<int64_t>();
  AclIntArrayView cu_seqlens_view(cu_seqlens_values);
  const auto chunk_indices_t = to<std::optional<Tensor>>(stack[2]);
  const std::vector<int64_t> chunk_indices_values = chunk_indices_t.has_value() ? host_int_values(chunk_indices_t->get()) : std::vector<int64_t>();
  AclIntArrayView chunk_indices_view(chunk_indices_values);
  const int64_t layout = to<int64_t>(stack[3]);
  const int64_t stream = to<int64_t>(stack[4]);

  auto& rt = Runtime::instance();
  auto get_ws = reinterpret_cast<npu_solve_tri_GetWorkspaceFn>(
      rt.symbol("aclnnSolveTriGetWorkspaceSize"));
  auto launch = reinterpret_cast<LaunchFn>(
      rt.symbol("aclnnSolveTri"));
  uint64_t workspace_size = 0;
  aclOpExecutor* executor = nullptr;

  std::vector<Tensor> outputs;
  std::vector<bool> output_present;
  outputs.push_back(allocate_like(x_meta));
  output_present.push_back(true);

  std::vector<std::unique_ptr<AclTensorView>> views;
  views.push_back(std::make_unique<AclTensorView>(x_meta));
  views.push_back(std::make_unique<AclTensorView>(meta_of(outputs[0])));

  const int get_ret = get_ws(
      views[0]->get(),
      cu_seqlens_view.get(),
      chunk_indices_view.get(),
      npu_solve_tri_layout_name(layout),
      views[1]->get(),
      &workspace_size, &executor);
  if (get_ret != 0) {
    throw std::runtime_error(
        "aclnnSolveTriGetWorkspaceSize failed: " +
        std::to_string(get_ret));
  }
  Tensor workspace;
  void* workspace_ptr = nullptr;
  if (workspace_size != 0) {
    workspace = allocate_bytes(static_cast<int64_t>(workspace_size),
                                   x_meta);
    TORCH_ERROR_CODE_CHECK(
        aoti_torch_get_data_ptr(workspace.get(), &workspace_ptr));
  }
  const int launch_ret = launch(workspace_ptr, workspace_size,
                                executor,
                                reinterpret_cast<void*>(stream));
  if (launch_ret != 0) {
    throw std::runtime_error(
        "aclnnSolveTri failed: " +
        std::to_string(launch_ret));
  }
  stack[0] = from(outputs[0]);
}


namespace {
// Registration hooks called from stable_ops.cpp's single def/impl blocks.
template <typename Library>
void register_generated_defs(Library& m) {

  m.def(kSchema_npu_causal_conv1d_bwd);
  m.def(kSchema_npu_chunk_bwd_dqkwg);
  m.def(kSchema_npu_chunk_bwd_dv_local);
  m.def(kSchema_npu_chunk_fwd_h);
  m.def(kSchema_npu_chunk_fwd_o);
  m.def(kSchema_npu_chunk_gated_delta_rule_bwd_dhu);
  m.def(kSchema_npu_chunk_gated_delta_rule_bwd_finalize);
  m.def(kSchema_npu_chunk_gated_delta_rule_fwd);
  m.def(kSchema_npu_chunk_gated_delta_rule_fwd_h);
  m.def(kSchema_npu_chunk_gated_delta_rule_fwd_prepare);
  m.def(kSchema_npu_chunk_kda_bwd);
  m.def(kSchema_npu_chunk_kda_bwd_intra);
  m.def(kSchema_npu_chunk_kda_fwd);
  m.def(kSchema_npu_chunk_local_cumsum);
  m.def(kSchema_npu_chunk_scaled_dot_kkt);
  m.def(kSchema_npu_fast_gelu_custom);
  m.def(kSchema_npu_fast_gelu_custom_backward);
  m.def(kSchema_npu_kda_gate_cumsum);
  m.def(kSchema_npu_prepare_wy_repr_bwd);
  m.def(kSchema_npu_prepare_wy_repr_bwd_da);
  m.def(kSchema_npu_prepare_wy_repr_bwd_full);
  m.def(kSchema_npu_recompute_w_u_fwd);
  m.def(kSchema_npu_solve_tri);
}

template <typename Library>
void register_generated_impls(Library& m) {
  m.impl("npu_causal_conv1d_bwd", &boxed_npu_causal_conv1d_bwd);
  m.impl("npu_chunk_bwd_dqkwg", &boxed_npu_chunk_bwd_dqkwg);
  m.impl("npu_chunk_bwd_dv_local", &boxed_npu_chunk_bwd_dv_local);
  m.impl("npu_chunk_fwd_h", &boxed_npu_chunk_fwd_h);
  m.impl("npu_chunk_fwd_o", &boxed_npu_chunk_fwd_o);
  m.impl("npu_chunk_gated_delta_rule_bwd_dhu", &boxed_npu_chunk_gated_delta_rule_bwd_dhu);
  m.impl("npu_chunk_gated_delta_rule_bwd_finalize", &boxed_npu_chunk_gated_delta_rule_bwd_finalize);
  m.impl("npu_chunk_gated_delta_rule_fwd", &boxed_npu_chunk_gated_delta_rule_fwd);
  m.impl("npu_chunk_gated_delta_rule_fwd_h", &boxed_npu_chunk_gated_delta_rule_fwd_h);
  m.impl("npu_chunk_gated_delta_rule_fwd_prepare", &boxed_npu_chunk_gated_delta_rule_fwd_prepare);
  m.impl("npu_chunk_kda_bwd", &boxed_npu_chunk_kda_bwd);
  m.impl("npu_chunk_kda_bwd_intra", &boxed_npu_chunk_kda_bwd_intra);
  m.impl("npu_chunk_kda_fwd", &boxed_npu_chunk_kda_fwd);
  m.impl("npu_chunk_local_cumsum", &boxed_npu_chunk_local_cumsum);
  m.impl("npu_chunk_scaled_dot_kkt", &boxed_npu_chunk_scaled_dot_kkt);
  m.impl("npu_fast_gelu_custom", &boxed_npu_fast_gelu_custom);
  m.impl("npu_fast_gelu_custom_backward", &boxed_npu_fast_gelu_custom_backward);
  m.impl("npu_kda_gate_cumsum", &boxed_npu_kda_gate_cumsum);
  m.impl("npu_prepare_wy_repr_bwd", &boxed_npu_prepare_wy_repr_bwd);
  m.impl("npu_prepare_wy_repr_bwd_da", &boxed_npu_prepare_wy_repr_bwd_da);
  m.impl("npu_prepare_wy_repr_bwd_full", &boxed_npu_prepare_wy_repr_bwd_full);
  m.impl("npu_recompute_w_u_fwd", &boxed_npu_recompute_w_u_fwd);
  m.impl("npu_solve_tri", &boxed_npu_solve_tri);
}
}  // namespace
